<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>使用 Tampermonkey 静默浏览器控制台绕过网站脚本检测</title>
      <link href="/20260827/console-silent-tampermonkey/"/>
      <url>/20260827/console-silent-tampermonkey/</url>
      
        <content type="html"><![CDATA[<h2 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h2><p>很多网站在检测到浏览器插件（如油猴脚本）注入控制台时，会弹出警告：”检测到您的浏览器插件正在注入异常脚本，请使用隐身模式打开页面！”</p><p>这是因为网站通过监听 <code>console</code> 对象的方法调用或 <code>error</code> 事件来检测是否有脚本在”窥探”或修改控制台行为。</p><hr><h2 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h2><ol><li><strong>拦截错误事件</strong>：阻止 <code>error</code> 和 <code>unhandledrejection</code> 事件的传播</li><li><strong>伪装 console 方法</strong>：用空函数替换所有 console 方法，并伪装成原生函数</li></ol><hr><h2 id="使用教程"><a href="#使用教程" class="headerlink" title="使用教程"></a>使用教程</h2><h3 id="粘贴脚本代码"><a href="#粘贴脚本代码" class="headerlink" title="粘贴脚本代码"></a>粘贴脚本代码</h3><p>将以下完整代码粘贴到编辑器中：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// ==UserScript==</span></span><br><span class="line"><span class="comment">// @name         静默控制台</span></span><br><span class="line"><span class="comment">// @namespace    http://tampermonkey.net/</span></span><br><span class="line"><span class="comment">// @version      1.0</span></span><br><span class="line"><span class="comment">// @description  完全静默浏览器控制台，阻止错误输出和日志打印</span></span><br><span class="line"><span class="comment">// @author       zxclf</span></span><br><span class="line"><span class="comment">// @match        *://*/*</span></span><br><span class="line"><span class="comment">// @run-at       document-start</span></span><br><span class="line"><span class="comment">// @grant        none</span></span><br><span class="line"><span class="comment">// @noframes</span></span><br><span class="line"><span class="comment">// ==/UserScript==</span></span><br><span class="line"></span><br><span class="line">(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="string">&#x27;use strict&#x27;</span>;</span><br><span class="line">    <span class="keyword">const</span> methods = [<span class="string">&#x27;log&#x27;</span>, <span class="string">&#x27;warn&#x27;</span>, <span class="string">&#x27;error&#x27;</span>, <span class="string">&#x27;info&#x27;</span>, <span class="string">&#x27;debug&#x27;</span>, <span class="string">&#x27;trace&#x27;</span>, <span class="string">&#x27;table&#x27;</span>, <span class="string">&#x27;dir&#x27;</span>, <span class="string">&#x27;dirxml&#x27;</span>, </span><br><span class="line">                     <span class="string">&#x27;group&#x27;</span>, <span class="string">&#x27;groupCollapsed&#x27;</span>, <span class="string">&#x27;groupEnd&#x27;</span>, <span class="string">&#x27;time&#x27;</span>, <span class="string">&#x27;timeEnd&#x27;</span>, <span class="string">&#x27;profile&#x27;</span>, <span class="string">&#x27;profileEnd&#x27;</span>, </span><br><span class="line">                     <span class="string">&#x27;count&#x27;</span>, <span class="string">&#x27;assert&#x27;</span>, <span class="string">&#x27;clear&#x27;</span>];</span><br><span class="line">    <span class="keyword">const</span> originalConsole = <span class="variable language_">window</span>.<span class="property">console</span>;</span><br><span class="line">    <span class="keyword">const</span> fakeCache = <span class="keyword">new</span> <span class="title class_">Map</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> fakeHandler = <span class="keyword">function</span>(<span class="params">e</span>) &#123;</span><br><span class="line">        e.<span class="title function_">stopImmediatePropagation</span>();</span><br><span class="line">        e.<span class="title function_">preventDefault</span>();</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(fakeHandler, <span class="string">&#x27;name&#x27;</span>, &#123; <span class="attr">value</span>: <span class="string">&#x27;onGlobalError&#x27;</span>, <span class="attr">configurable</span>: <span class="literal">false</span> &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;error&#x27;</span>, fakeHandler, <span class="literal">true</span>);</span><br><span class="line">    <span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;unhandledrejection&#x27;</span>, fakeHandler, <span class="literal">true</span>);</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">window</span>.<span class="property">console</span> = <span class="keyword">new</span> <span class="title class_">Proxy</span>(originalConsole, &#123;</span><br><span class="line">        <span class="title function_">get</span>(<span class="params">target, prop, receiver</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (methods.<span class="title function_">includes</span>(prop) &amp;&amp; <span class="keyword">typeof</span> target[prop] === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (!fakeCache.<span class="title function_">has</span>(prop)) &#123;</span><br><span class="line">                    <span class="keyword">const</span> originalFn = target[prop];</span><br><span class="line">                    <span class="keyword">const</span> fake = <span class="keyword">function</span>(<span class="params"></span>) &#123;&#125;;</span><br><span class="line">                    <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(fake, <span class="string">&#x27;name&#x27;</span>, &#123;</span><br><span class="line">                        <span class="attr">value</span>: prop,</span><br><span class="line">                        <span class="attr">configurable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">enumerable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">writable</span>: <span class="literal">false</span></span><br><span class="line">                    &#125;);</span><br><span class="line">                    <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(fake, <span class="string">&#x27;length&#x27;</span>, &#123;</span><br><span class="line">                        <span class="attr">value</span>: originalFn.<span class="property">length</span>,</span><br><span class="line">                        <span class="attr">configurable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">enumerable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">writable</span>: <span class="literal">false</span></span><br><span class="line">                    &#125;);</span><br><span class="line">                    <span class="keyword">const</span> nativeStr = <span class="string">`function <span class="subst">$&#123;prop&#125;</span>() &#123; [native code] &#125;`</span>;</span><br><span class="line">                    <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(fake, <span class="string">&#x27;toString&#x27;</span>, &#123;</span><br><span class="line">                        <span class="attr">value</span>: <span class="function">() =&gt;</span> nativeStr,</span><br><span class="line">                        <span class="attr">configurable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">enumerable</span>: <span class="literal">false</span>,</span><br><span class="line">                        <span class="attr">writable</span>: <span class="literal">false</span></span><br><span class="line">                    &#125;);</span><br><span class="line">                    fakeCache.<span class="title function_">set</span>(prop, fake);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> fakeCache.<span class="title function_">get</span>(prop);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">get</span>(target, prop, receiver);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure><h3 id="保存并启用脚本"><a href="#保存并启用脚本" class="headerlink" title="保存并启用脚本"></a>保存并启用脚本</h3><ol><li>按 <code>Ctrl+S</code>（Windows&#x2F;Linux）或 <code>Cmd+S</code>（Mac）保存</li><li>或者点击工具栏的 <strong>“文件” → “保存”</strong></li></ol><h3 id="测试效果"><a href="#测试效果" class="headerlink" title="测试效果"></a>测试效果</h3><ol><li>打开之前报错的网站</li><li>按 <code>F12</code> 打开开发者工具</li><li>切换到 Console（控制台）选项卡</li><li>错误页面不会再弹出”检测到插件注入”的警告</li></ol><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>脚本通过”静默”手段让网站无法检测到插件的存在，从而绕过”请使用隐身模式”的警告。它适用于绝大多数基于 console 检测的网站。</p>]]></content>
      
      
      <categories>
          
          <category> 技术教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Tampermonkey </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>SSH 完全教程：从零基础到跳板机与隧道转发</title>
      <link href="/20260726/ssh-tutorial-bastion-proxy/"/>
      <url>/20260726/ssh-tutorial-bastion-proxy/</url>
      
        <content type="html"><![CDATA[<p>SSH（Secure Shell）是远程管理服务器、传输文件和构建安全隧道的工具。本教程将从零开始，覆盖 Linux、macOS 和 Windows 三大平台，带你从基础登录一路进阶到跳板、隧道和连接复用等高级技巧。</p><hr><h2 id="SSH-是什么？"><a href="#SSH-是什么？" class="headerlink" title="SSH 是什么？"></a>SSH 是什么？</h2><p>SSH 是一种加密网络协议，用于在不安全的网络中安全地访问远程计算机。它的核心目的是<strong>替代不安全的 Telnet、rlogin、FTP 等</strong>，提供强加密的远程 shell 和文件传输。SSH 目前使用的主要版本为 <strong>SSH-2</strong>（SSH-1 因安全缺陷已被淘汰），本教程所有内容均基于 SSH-2。</p><p><strong>工作简理</strong><br>SSH 连接分为三个阶段：</p><ol><li><strong>传输层建立</strong>：客户端与服务器协商加密算法，通过 <code>Diffie‑Hellman</code> 密钥交换生成会话密钥，后续通信全部加密。</li><li><strong>用户认证</strong>：服务器向客户端提供自己的主机密钥指纹供验证（防止中间人攻击），客户端再向服务器证明自己的身份，最常用的是密码和公钥认证。</li><li><strong>服务会话</strong>：认证通过后，打开加密通道，可以运行远程 shell、执行命令、转发端口等。</li></ol><p>本教程的命令均基于 <strong>OpenSSH</strong>，它是全球 Linux、macOS、Windows 的标准 SSH 实现，也是其他 SSH 工具的底层基础。</p><hr><h2 id="安装-SSH"><a href="#安装-SSH" class="headerlink" title="安装 SSH"></a>安装 SSH</h2><p>我们分别说明客户端的安装，如果想让本机作为 SSH 服务器被连接，则需要额外安装并启用 <code>sshd</code> 服务。</p><h3 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h3><p>绝大多数现代发行版已预装 OpenSSH 客户端，可通过以下命令检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -V</span><br></pre></td></tr></table></figure><p>若未安装或需要最新版本：</p><ul><li><strong>Debian &#x2F; Ubuntu &#x2F; Linux Mint</strong>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update &amp;&amp; <span class="built_in">sudo</span> apt install openssh-client</span><br></pre></td></tr></table></figure></li><li><strong>Red Hat &#x2F; CentOS &#x2F; Fedora &#x2F; RHEL</strong>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> dnf install openssh-clients</span><br></pre></td></tr></table></figure></li><li><strong>Arch Linux &#x2F; Manjaro</strong>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S openssh</span><br></pre></td></tr></table></figure></li><li><strong>openSUSE</strong>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zypper install openssh</span><br></pre></td></tr></table></figure></li></ul><blockquote><p>若想让本机作为 <strong>SSH 服务器</strong> 被连接，需要安装 <code>openssh-server</code> 并启动服务（如 <code>sudo systemctl enable ssh --now</code>）。详细服务器配置请见第 8 节。</p></blockquote><h3 id="macOS"><a href="#macOS" class="headerlink" title="macOS"></a>macOS</h3><p>macOS 从很早的版本就自带了 OpenSSH 客户端和服务器，终端直接可用，无需安装任何东西。可运行 <code>ssh -V</code> 确认。如果需要更新版本，最方便的是通过 Homebrew：<code>brew install openssh</code>（它不会替换系统自带版本，但会让你使用最新的 <code>ssh</code>）。系统自带的 sshd 服务默认未开启，如需开启可在“系统设置 → 通用 → 共享”中勾选“远程登录”，或使用 <code>sudo systemsetup -setremotelogin on</code>。详细配置见第 8 节。</p><h3 id="Windows"><a href="#Windows" class="headerlink" title="Windows"></a>Windows</h3><p><strong>方法一：内置 OpenSSH（推荐）</strong><br>Windows 10 1809 及以上、Windows Server 2019 及以上已内置 OpenSSH 客户端。</p><ul><li>在 <strong>PowerShell</strong> 或 <strong>命令提示符</strong> 中直接运行 <code>ssh</code> 测试是否可用。</li><li>若未启用，通过 <strong>设置 → 应用 → 可选功能 → 添加功能 → OpenSSH 客户端</strong> 安装。</li><li>也可通过 PowerShell 管理员命令安装：<figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Add-WindowsCapability</span> <span class="literal">-Online</span> <span class="literal">-Name</span> OpenSSH.Client~~~~<span class="number">0.0</span>.<span class="number">1.0</span></span><br></pre></td></tr></table></figure></li></ul><p><strong>方法二：WSL（Windows Subsystem for Linux）</strong><br>安装 WSL2 后，你将获得一个完整的 Linux 环境，其内的 OpenSSH 与 Linux 完全一致。日常管理工作可以将配置集中放在 WSL 里，并通过 <code>\\wsl$</code> 互访文件。</p><p><strong>方法三：第三方图形客户端</strong><br>如 PuTTY、MobaXterm、Termius 等，它们提供图形界面，适合不习惯命令行的用户。本教程主要聚焦命令行（OpenSSH）的统一用法，学会后所有平台通用。</p><hr><h2 id="基本用法：第一次连接"><a href="#基本用法：第一次连接" class="headerlink" title="基本用法：第一次连接"></a>基本用法：第一次连接</h2><p>最基础的用法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ssh username@remote_host</span><br><span class="line"><span class="comment"># 例如</span></span><br><span class="line">ssh root@192.168.1.100</span><br></pre></td></tr></table></figure><p>如果 SSH 服务端口不是默认的 <strong>22</strong>，使用 <code>-p</code> 指定（注意是小写 p）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -p 2222 user@host</span><br></pre></td></tr></table></figure><h3 id="首次连接与主机密钥"><a href="#首次连接与主机密钥" class="headerlink" title="首次连接与主机密钥"></a>首次连接与主机密钥</h3><p>第一次连接时，你会看到类似信息：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">The authenticity of host &#x27;example.com (93.184.216.34)&#x27; can&#x27;t be established.</span><br><span class="line">ED25519 key fingerprint is SHA256:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.</span><br><span class="line">Are you sure you want to continue connecting (yes/no)?</span><br></pre></td></tr></table></figure><p>这是 <strong>SSH 主机密钥验证</strong>，目的是防止中间人攻击。你应当通过可信渠道（如控制台、带外通信）核对这个指纹，确认无误后输入 <code>yes</code>。一旦确认，该指纹被保存在：</p><ul><li>Linux &#x2F; macOS：<code>~/.ssh/known_hosts</code></li><li>Windows：<code>C:\Users\你的用户名\.ssh\known_hosts</code></li></ul><p>后续连接若指纹发生变化（服务器重装系统或发生攻击），你会得到醒目的警告并拒绝连接。如果确认是正常变更，可以用下面命令清除旧指纹：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -R 主机名或IP</span><br></pre></td></tr></table></figure><p>输入 <code>yes</code> 后，会提示你输入远程用户的密码，密码输入时不显示字符，输入完按回车即可登录。不过密码登录既麻烦又有被暴力破解的风险，因此<strong>强烈建议立即设置密钥登录</strong>。</p><hr><h2 id="密钥认证（免密登录）"><a href="#密钥认证（免密登录）" class="headerlink" title="密钥认证（免密登录）"></a>密钥认证（免密登录）</h2><p>公钥认证是目前最安全且方便的 SSH 认证方式。原理：你生成一对密钥（私钥自己保管，公钥放到服务器上），连接时客户端用私钥签名一个挑战，服务器用公钥验证签名。</p><h3 id="生成密钥对"><a href="#生成密钥对" class="headerlink" title="生成密钥对"></a>生成密钥对</h3><p>推荐使用更安全的 <strong>ED25519</strong> 算法（速度快、密钥短、安全强度相当于 RSA 3072 位）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;your_email@example.com&quot;</span></span><br></pre></td></tr></table></figure><p><code>-C</code> 后面跟的注释可随意填写，通常用于标识密钥来源。</p><p>如果您的系统或目标服务器较旧不支持 ED25519，可降级使用 RSA：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t rsa -b 4096 -C <span class="string">&quot;your_email@example.com&quot;</span></span><br></pre></td></tr></table></figure><p>命令会提示你：</p><ul><li><strong>保存路径</strong>：直接回车使用默认 <code>~/.ssh/id_ed25519</code>（私钥）和 <code>~/.ssh/id_ed25519.pub</code>（公钥）。</li><li><strong>passphrase</strong>：强烈建议设置一个强密码短语。即使私钥泄露，没有 passphrase 也无法直接使用。如果不希望每次都输入，可配合 <code>ssh-agent</code> 使用（见后文）。留空则无密码保护。</li></ul><h3 id="将公钥复制到远程服务器"><a href="#将公钥复制到远程服务器" class="headerlink" title="将公钥复制到远程服务器"></a>将公钥复制到远程服务器</h3><p>最简单的方式是使用 <code>ssh-copy-id</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-copy-id username@remote_host</span><br></pre></td></tr></table></figure><p>该命令会自动登录服务器，将本机默认公钥追加到远程的 <code>~/.ssh/authorized_keys</code>，并自动纠正目录和文件的权限。</p><p>若系统无 <code>ssh-copy-id</code>（某些精简版或 Windows 环境），可手动复制：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 通用一行命令</span></span><br><span class="line"><span class="built_in">cat</span> ~/.ssh/id_ed25519.pub | ssh user@host <span class="string">&quot;mkdir -p ~/.ssh &amp;&amp; cat &gt;&gt; ~/.ssh/authorized_keys&quot;</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：必须使用 <code>&gt;&gt;</code> 追加，不能只用 <code>&gt;</code> 覆盖已有密钥。</p><h3 id="权限设置"><a href="#权限设置" class="headerlink" title="权限设置"></a>权限设置</h3><p>远程服务器上的 SSH 对目录和文件权限非常敏感。如果权限不正确，密钥认证会<strong>静默失败</strong>。在远程主机上执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 700 ~/.ssh</span><br><span class="line"><span class="built_in">chmod</span> 600 ~/.ssh/authorized_keys</span><br></pre></td></tr></table></figure><p>本地私钥的权限同样需要是 <code>600</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 600 ~/.ssh/id_ed25519</span><br><span class="line"><span class="built_in">chmod</span> 644 ~/.ssh/id_ed25519.pub  <span class="comment"># 公钥可为 644</span></span><br></pre></td></tr></table></figure><p>在 Windows 上使用内置 OpenSSH，权限管理机制不同，但同样要求<strong>只有当前用户和 SYSTEM 可以访问私钥</strong>（通过文件属性 → 安全 → 高级，移除其他账户权限）。</p><h3 id="管理多个密钥"><a href="#管理多个密钥" class="headerlink" title="管理多个密钥"></a>管理多个密钥</h3><p>当你有多个服务器或不同用途的密钥时，可以生成并保存为不同文件名，然后连接时用 <code>-i</code> 指定：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -i ~/.ssh/my_ec2_key.pem ubuntu@ec2-host</span><br></pre></td></tr></table></figure><p>更优雅的方式是在 <code>~/.ssh/config</code> 中为不同主机指定 <code>IdentityFile</code>，见下一章。</p><hr><h2 id="SSH-客户端配置文件-ssh-config"><a href="#SSH-客户端配置文件-ssh-config" class="headerlink" title="SSH 客户端配置文件 ~/.ssh/config"></a>SSH 客户端配置文件 <code>~/.ssh/config</code></h2><p>通过配置文件，可以为不同主机定义别名、端口、用户名、密钥路径，甚至高级代理规则，让连接变得异常简单。</p><p>配置文件位置：</p><ul><li>Linux &#x2F; macOS：<code>~/.ssh/config</code></li><li>Windows：<code>C:\Users\你的用户名\.ssh\config</code> （PowerShell 中也可用 <code>~/.ssh/config</code>）</li></ul><p>该文件由多个 <code>Host</code> 块组成。解析顺序从上到下，匹配第一个符合的配置。</p><p><strong>基础配置示例：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"># 一个开发服务器</span><br><span class="line">Host dev</span><br><span class="line">    HostName 192.168.1.100</span><br><span class="line">    User root</span><br><span class="line">    Port 2222</span><br><span class="line">    IdentityFile ~/.ssh/dev_ed25519</span><br><span class="line"></span><br><span class="line"># 跳板机</span><br><span class="line">Host jump</span><br><span class="line">    HostName jump.example.com</span><br><span class="line">    User admin</span><br><span class="line"></span><br><span class="line"># 内网主机，通过跳板机连接</span><br><span class="line">Host internal</span><br><span class="line">    HostName 10.0.0.5</span><br><span class="line">    User ubuntu</span><br><span class="line">    ProxyJump jump    # 自动先连 jump 再连此主机</span><br><span class="line">    ForwardAgent no   # 明确禁用 agent 转发</span><br></pre></td></tr></table></figure><p>配置完成后，直接 <code>ssh dev</code> 即可连接；<code>ssh internal</code> 则自动通过跳板机。</p><p><strong>常用配置选项：</strong></p><table><thead><tr><th>选项</th><th>说明</th></tr></thead><tbody><tr><td>Host</td><td>别名或通配符 <code>*</code>，<code>*.example.com</code></td></tr><tr><td>HostName</td><td>实际主机名或 IP</td></tr><tr><td>User</td><td>登录用户名</td></tr><tr><td>Port</td><td>端口号</td></tr><tr><td>IdentityFile</td><td>指定私钥路径，可多个</td></tr><tr><td>ProxyJump</td><td>跳板主机，可多个（逗号分隔）</td></tr><tr><td>ForwardAgent yes&#x2F;no</td><td>是否启用 agent 转发</td></tr><tr><td>Compression yes</td><td>开启压缩（低带宽时有用）</td></tr><tr><td>ServerAliveInterval</td><td>心跳间隔（秒）</td></tr><tr><td>ControlMaster auto</td><td>启用连接复用</td></tr><tr><td>ControlPath</td><td>控制套接字路径</td></tr><tr><td>LogLevel INFO</td><td>输出日志级别，调试时用 DEBUG1</td></tr></tbody></table><p><strong>通配符示例：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Host *.mydomain.com</span><br><span class="line">    User admin</span><br><span class="line">    IdentityFile ~/.ssh/corp_ed25519</span><br></pre></td></tr></table></figure><p>任何匹配 <code>*.mydomain.com</code> 的主机都会自动应用此配置。</p><hr><h2 id="文件传输：scp、rsync、sftp"><a href="#文件传输：scp、rsync、sftp" class="headerlink" title="文件传输：scp、rsync、sftp"></a>文件传输：scp、rsync、sftp</h2><h3 id="scp（Secure-Copy）"><a href="#scp（Secure-Copy）" class="headerlink" title="scp（Secure Copy）"></a>scp（Secure Copy）</h3><p>scp 是 SSH 协议的原生文件传输工具，用法和 <code>cp</code> 类似。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 上传文件（注意 -P 大写指定端口）</span></span><br><span class="line">scp -P 2222 local_file.txt user@host:/remote/path/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载文件</span></span><br><span class="line">scp user@host:/remote/file.txt ./local_folder/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 递归复制整个目录</span></span><br><span class="line">scp -r my_project user@host:/opt/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 跨服务器复制（流量从 server1 直接到 server2，不经过本机）</span></span><br><span class="line">scp user@server1:/path/file user@server2:/another/path/</span><br></pre></td></tr></table></figure><p>其他常用参数：</p><ul><li><code>-C</code>：传输时压缩</li><li><code>-p</code>：保留原文件的修改时间和权限</li><li><code>-v</code>：显示详细过程</li></ul><h3 id="rsync（增量同步，更高效）"><a href="#rsync（增量同步，更高效）" class="headerlink" title="rsync（增量同步，更高效）"></a>rsync（增量同步，更高效）</h3><p>rsync 通过 SSH 通道同步文件和目录，只传输差异部分，适合大批量文件或定时备份。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 同步本地目录到远程（-a 归档模式，-v 显示详情，-z 压缩）</span></span><br><span class="line">rsync -avz -e <span class="string">&quot;ssh -p 2222&quot;</span> local_dir/ user@host:/remote_dir/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 从远程同步到本地</span></span><br><span class="line">rsync -avz user@host:/remote/dir/ ./local_dir/</span><br><span class="line"></span><br><span class="line"><span class="comment"># 删除目标端多余文件，完全镜像</span></span><br><span class="line">rsync -avz --delete -e ssh <span class="built_in">local</span>/ user@host:/backup/</span><br></pre></td></tr></table></figure><p>注意：本地源路径末尾加 <code>/</code> 表示复制目录<em>内容</em>，不加 <code>/</code> 则连同目录本身一起复制。</p><p>配合 <code>~/.ssh/config</code> 使用会更简单，因为 rsync 会继承 SSH 配置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rsync -avz dev:/data/backup/ ./backup/</span><br></pre></td></tr></table></figure><h3 id="SFTP（交互式文件管理）"><a href="#SFTP（交互式文件管理）" class="headerlink" title="SFTP（交互式文件管理）"></a>SFTP（交互式文件管理）</h3><p>sftp 提供了一个类似 FTP 的交互式会话，但全程通过 SSH 加密。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">sftp user@host</span><br><span class="line"><span class="comment"># 或者使用配置文件中的别名</span></span><br><span class="line">sftp dev</span><br></pre></td></tr></table></figure><p>进入后常用命令：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">ls, cd, pwd          -- 操作远程目录</span><br><span class="line">lls, lcd, lpwd       -- 操作本地目录（前缀 l 表示 local）</span><br><span class="line">put file              -- 上传文件</span><br><span class="line">get remotefile        -- 下载文件</span><br><span class="line">mput *.txt            -- 批量上传</span><br><span class="line">mget *.log            -- 批量下载</span><br><span class="line">!command              -- 执行本地 shell 命令</span><br><span class="line">exit                  -- 退出</span><br></pre></td></tr></table></figure><p>sftp 同样可以使用 <code>-P</code> 指定端口，也完全遵循 <code>~/.ssh/config</code> 中的配置。</p><hr><h2 id="SSH-端口转发与隧道"><a href="#SSH-端口转发与隧道" class="headerlink" title="SSH 端口转发与隧道"></a>SSH 端口转发与隧道</h2><p>SSH 最强大的功能之一：将任何 TCP 流量封装在加密隧道中。无论是保护不安全的协议，还是穿越防火墙访问内网，都非常实用。</p><h3 id="本地转发（-L）"><a href="#本地转发（-L）" class="headerlink" title="本地转发（-L）"></a>本地转发（<code>-L</code>）</h3><p>把本地计算机的某个端口，通过 SSH 服务器转发到目标地址。</p><p><strong>格式：</strong><br><code>ssh -L 本地端口:目标主机:目标端口 用户@SSH服务器</code></p><p><strong>典型场景：</strong> 远程 SSH 服务器 <code>server</code> 能访问内网数据库 <code>db.internal:3306</code>，而你的本地笔记本无法直连内网。你可以这样：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -L 3307:db.internal:3306 user@server</span><br></pre></td></tr></table></figure><p>现在，本地 <code>localhost:3307</code> 的所有流量都会被加密后送到 <code>server</code>，再由 <code>server</code> 转发给 <code>db.internal:3306</code>。你在本机用数据库客户端连接 <code>127.0.0.1:3307</code> 即可。</p><p><strong>多主机、多端口可以叠加</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -L 8080:intra-web:80 -L 8443:intra-web:443 user@gateway</span><br></pre></td></tr></table></figure><h3 id="远程转发（-R）"><a href="#远程转发（-R）" class="headerlink" title="远程转发（-R）"></a>远程转发（<code>-R</code>）</h3><p>与本地转发相反，它把远程 SSH 服务器上的端口流量，转到本地或本地能访问的主机。</p><p><strong>格式：</strong><br><code>ssh -R 远程端口:目标主机:目标端口 用户@SSH服务器</code></p><p><strong>示例：</strong> 你本地开发了一个 Web 应用监在 <code>localhost:3000</code>，想让远程搭档临时访问。执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -R 8080:localhost:3000 user@public-server</span><br></pre></td></tr></table></figure><p>之后，你的搭档在 <code>public-server</code> 上访问 <code>localhost:8080</code> 就能看到你本机的 Web 服务。</p><p><strong>注意</strong>：默认情况下，远程绑定的端口只能由 SSH 服务器本机（127.0.0.1）访问。如果希望外网也能访问该远程端口，需要在服务器 <code>/etc/ssh/sshd_config</code> 中配置 <code>GatewayPorts yes</code>，然后重启 sshd。此时远程绑定的地址变为 <code>0.0.0.0</code>。</p><h3 id="动态转发（-D）——-SOCKS-代理"><a href="#动态转发（-D）——-SOCKS-代理" class="headerlink" title="动态转发（-D）—— SOCKS 代理"></a>动态转发（<code>-D</code>）—— SOCKS 代理</h3><p>将 SSH 客户端变成一个 <strong>SOCKS5 代理服务器</strong>，所有支持 SOCKS 的应用程序都能通过该隧道访问网络。这是最常用的“安全上网”或“翻墙”方法。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -D 1080 user@server</span><br></pre></td></tr></table></figure><p>然后在操作系统的网络代理设置中填入 SOCKS5 代理：<code>127.0.0.1</code> 端口 <code>1080</code>。或者在浏览器（如 Firefox）中单独设置 SOCKS5 代理，并可勾选“通过代理发送 DNS 请求”以避免 DNS 泄露。</p><p>配合 <code>~/.ssh/config</code>，可以创建专门的代理主机别名：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Host proxy</span><br><span class="line">    HostName your-vps.com</span><br><span class="line">    User user</span><br><span class="line">    DynamicForward 1080</span><br><span class="line">    ServerAliveInterval 60</span><br></pre></td></tr></table></figure><p>然后只需 <code>ssh proxy</code>，后台保持连接，代理就绪。</p><hr><h2 id="SSH-服务器安装与配置"><a href="#SSH-服务器安装与配置" class="headerlink" title="SSH 服务器安装与配置"></a>SSH 服务器安装与配置</h2><p>如果你需要让自己的机器成为 SSH 服务器（让别人或自己远程连接），就必须安装并正确配置 OpenSSH 服务端（sshd）。本节将详细说明安装、基本配置与安全加固。</p><h3 id="安装-SSH-服务器"><a href="#安装-SSH-服务器" class="headerlink" title="安装 SSH 服务器"></a>安装 SSH 服务器</h3><p><strong>Linux</strong><br>根据发行版使用对应的包管理器安装 <code>openssh-server</code>：</p><ul><li>Debian &#x2F; Ubuntu：<code>sudo apt install openssh-server</code></li><li>RHEL &#x2F; CentOS &#x2F; Fedora：<code>sudo dnf install openssh-server</code></li><li>Arch Linux：<code>sudo pacman -S openssh</code>（已包含服务端）</li></ul><p>安装后启动并设置开机自启：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> ssh --now   <span class="comment"># 部分系统服务名为 sshd，请确认</span></span><br></pre></td></tr></table></figure><p><strong>macOS</strong><br>系统自带 sshd，开启方式：</p><ul><li>图形界面：<strong>系统设置 → 通用 → 共享 → 远程登录</strong>，勾选并选择允许的用户。</li><li>命令行：<code>sudo systemsetup -setremotelogin on</code>（关闭则为 <code>off</code>）。<br>通过 <code>sudo launchctl list | grep ssh</code> 检查服务状态。</li></ul><p><strong>Windows</strong><br>Windows Server 2019 &#x2F; Windows 10 1809 及以上可安装 OpenSSH 服务器：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 以管理员身份运行 PowerShell</span></span><br><span class="line"><span class="built_in">Add-WindowsCapability</span> <span class="literal">-Online</span> <span class="literal">-Name</span> OpenSSH.Server~~~~<span class="number">0.0</span>.<span class="number">1.0</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 启动并设置自动运行</span></span><br><span class="line"><span class="built_in">Start-Service</span> sshd</span><br><span class="line"><span class="built_in">Set-Service</span> <span class="literal">-Name</span> sshd <span class="literal">-StartupType</span> <span class="string">&#x27;Automatic&#x27;</span></span><br></pre></td></tr></table></figure><p>确认防火墙规则已自动创建（通常名为“OpenSSH SSH Server”），若未创建则手动允许端口 22。</p><h3 id="基础配置"><a href="#基础配置" class="headerlink" title="基础配置"></a>基础配置</h3><p>SSH 服务器的主配置文件：</p><ul><li>Linux &#x2F; macOS：<code>/etc/ssh/sshd_config</code></li><li>Windows：<code>%ProgramData%\ssh\sshd_config</code></li></ul><p>修改配置文件前，建议先备份：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> /etc/ssh/sshd_config /etc/ssh/sshd_config.bak</span><br></pre></td></tr></table></figure><p>常见基础选项（编辑后需重启服务生效）：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 监听端口（可改为非默认端口以降低扫描风险）</span></span><br><span class="line">Port 22</span><br><span class="line"></span><br><span class="line"><span class="comment"># 监听地址（默认 0.0.0.0 所有接口，可限定 IP）</span></span><br><span class="line">ListenAddress 0.0.0.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 允许或禁止 root 直接登录（强烈建议 no）</span></span><br><span class="line">PermitRootLogin no</span><br><span class="line"></span><br><span class="line"><span class="comment"># 最大认证尝试次数</span></span><br><span class="line">MaxAuthTries 6</span><br><span class="line"></span><br><span class="line"><span class="comment"># 登录宽限期（秒），超时断开未完成认证的连接</span></span><br><span class="line">LoginGraceTime 30</span><br><span class="line"></span><br><span class="line"><span class="comment"># 最大并发未认证连接数</span></span><br><span class="line">MaxStartups 10:30:100</span><br><span class="line"></span><br><span class="line"><span class="comment"># 是否允许密码认证</span></span><br><span class="line">PasswordAuthentication yes</span><br><span class="line"></span><br><span class="line"><span class="comment"># 是否允许公钥认证</span></span><br><span class="line">PubkeyAuthentication yes</span><br><span class="line"></span><br><span class="line"><span class="comment"># 授权密钥文件路径</span></span><br><span class="line">AuthorizedKeysFile .ssh/authorized_keys</span><br><span class="line"></span><br><span class="line"><span class="comment"># 是否允许空密码</span></span><br><span class="line">PermitEmptyPasswords no</span><br><span class="line"></span><br><span class="line"><span class="comment"># 是否允许 TCP 转发（根据需求开启）</span></span><br><span class="line">AllowTcpForwarding yes</span><br><span class="line"></span><br><span class="line"><span class="comment"># 是否允许 X11 转发（通常桌面环境需要时再开）</span></span><br><span class="line">X11Forwarding no</span><br><span class="line"></span><br><span class="line"><span class="comment"># 日志级别</span></span><br><span class="line">LogLevel INFO</span><br></pre></td></tr></table></figure><h3 id="密钥认证与禁用密码登录"><a href="#密钥认证与禁用密码登录" class="headerlink" title="密钥认证与禁用密码登录"></a>密钥认证与禁用密码登录</h3><p>为了安全，推荐只使用密钥登录。首先确保你能用密钥成功登录（见第 4 节），然后修改 <code>sshd_config</code>：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PasswordAuthentication no</span><br><span class="line">ChallengeResponseAuthentication no</span><br><span class="line">PubkeyAuthentication yes</span><br></pre></td></tr></table></figure><p>保存后重启服务。此后所有用户都必须使用密钥认证，密码登录将被彻底拒绝。</p><h3 id="安全加固"><a href="#安全加固" class="headerlink" title="安全加固"></a>安全加固</h3><p>除了禁用密码和 root 登录外，以下措施可大幅提高服务器安全性：</p><p><strong>更改默认端口</strong>  </p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Port 2222</span><br></pre></td></tr></table></figure><p>更改后需要在防火墙放行新端口，客户端连接时使用 <code>-p 2222</code>。</p><p><strong>限制可登录用户</strong>  </p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">AllowUsers alice bob</span><br><span class="line"><span class="comment"># 或使用组限制</span></span><br><span class="line">AllowGroups sshusers</span><br></pre></td></tr></table></figure><p><strong>指定现代安全算法</strong><br>在 <code>sshd_config</code> 中明确禁用老旧不安全算法：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group-exchange-sha256</span><br><span class="line">Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com</span><br><span class="line">MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com</span><br></pre></td></tr></table></figure><p><strong>禁用短密钥与协议 1</strong><br>确保以下配置存在：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Protocol 2</span><br><span class="line">HostKeyAlgorithms ssh-ed25519,rsa-sha2-512,rsa-sha2-256</span><br></pre></td></tr></table></figure><p><strong>空闲超时与环境隔离</strong>  </p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 客户端无操作多少秒后自动断开</span></span><br><span class="line">ClientAliveInterval 300</span><br><span class="line">ClientAliveCountMax 3</span><br><span class="line"><span class="comment"># 为 sftp 用户限制目录（可选）</span></span><br><span class="line">Subsystem sftp internal-sftp</span><br><span class="line">Match Group sftpusers</span><br><span class="line">    ChrootDirectory /home/%u</span><br><span class="line">    ForceCommand internal-sftp</span><br><span class="line">    AllowTcpForwarding no</span><br></pre></td></tr></table></figure><h3 id="防火墙与-SELinux"><a href="#防火墙与-SELinux" class="headerlink" title="防火墙与 SELinux"></a>防火墙与 SELinux</h3><p>确保服务器防火墙允许 SSH 端口（默认 22，或自定义端口）：</p><ul><li><strong>ufw</strong>（Ubuntu 等）：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ufw allow 22/tcp</span><br><span class="line"><span class="comment"># 或自定义端口</span></span><br><span class="line"><span class="built_in">sudo</span> ufw allow 2222/tcp</span><br><span class="line"><span class="built_in">sudo</span> ufw <span class="built_in">enable</span></span><br></pre></td></tr></table></figure></li><li><strong>firewalld</strong>（CentOS&#x2F;RHEL）：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> firewall-cmd --add-port=22/tcp --permanent</span><br><span class="line"><span class="built_in">sudo</span> firewall-cmd --reload</span><br></pre></td></tr></table></figure></li><li><strong>iptables</strong>：直接添加规则或使用 <code>iptables-persistent</code>。</li></ul><p>如果启用了 SELinux，需要允许 SSH 使用非标准端口（例如 2222）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> semanage port -a -t ssh_port_t -p tcp 2222</span><br></pre></td></tr></table></figure><h3 id="配置测试与服务管理"><a href="#配置测试与服务管理" class="headerlink" title="配置测试与服务管理"></a>配置测试与服务管理</h3><p>每次修改 <code>sshd_config</code> 后，先用测试模式验证语法，然后再重启服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> sshd -t</span><br></pre></td></tr></table></figure><p>若输出为空则表示配置正确；若有错误会提示具体行号。</p><p>管理 SSH 服务的常用命令（Linux）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl restart sshd    <span class="comment"># 重启</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl status sshd     <span class="comment"># 查看状态</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl reload sshd     <span class="comment"># 重载配置（不中断现有连接，适用于部分参数）</span></span><br></pre></td></tr></table></figure><p>Windows 上对应 PowerShell 命令：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Restart-Service</span> sshd</span><br><span class="line"><span class="built_in">Get-Service</span> sshd</span><br></pre></td></tr></table></figure><h3 id="查看日志"><a href="#查看日志" class="headerlink" title="查看日志"></a>查看日志</h3><p>连接出现问题时的排查思路：</p><ul><li><strong>Linux</strong>：根据发行版查看 <code>/var/log/auth.log</code>（Debian&#x2F;Ubuntu）或 <code>/var/log/secure</code>（RHEL&#x2F;CentOS）。<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">tail</span> -f /var/log/auth.log</span><br></pre></td></tr></table></figure></li><li><strong>macOS</strong>：使用 <code>log show --predicate &#39;process == &quot;sshd&quot;&#39; --last 1h</code> 或查看 <code>/var/log/system.log</code>。</li><li><strong>Windows</strong>：通过“事件查看器”进入“应用程序和服务日志 → OpenSSH → 操作”，或使用 PowerShell：<figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Get-WinEvent</span> <span class="literal">-LogName</span> <span class="string">&quot;OpenSSH/Operational&quot;</span> | <span class="built_in">Select-Object</span> <span class="literal">-Last</span> <span class="number">20</span></span><br></pre></td></tr></table></figure>日志中会详细记录认证失败的原因、权限问题、算法协商错误等。</li></ul><p><strong>8.8 额外防护工具</strong><br>强烈建议部署 <strong>fail2ban</strong>，它能检测暴力破解并动态封锁 IP：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install fail2ban</span><br><span class="line"><span class="comment"># 创建并编辑本地配置</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> /etc/fail2ban/jail.conf /etc/fail2ban/jail.local</span><br><span class="line"><span class="comment"># 在 [sshd] 部分设置 enabled = true，根据需要调整 bantime、maxretry 等</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl restart fail2ban</span><br></pre></td></tr></table></figure><p>还可结合双因素认证（如 Google Authenticator + PAM）进一步增强安全性。</p><hr><h2 id="高级用法"><a href="#高级用法" class="headerlink" title="高级用法"></a>高级用法</h2><h3 id="跳板机（ProxyJump）"><a href="#跳板机（ProxyJump）" class="headerlink" title="跳板机（ProxyJump）"></a>跳板机（ProxyJump）</h3><p>在内网环境中，你往往需要先登录一台暴露在公网的堡垒机，再通过它访问内部的机器。<code>ProxyJump</code> 让这一过程全透明。</p><p><strong>配置文件方式：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Host bastion</span><br><span class="line">    HostName jump.example.com</span><br><span class="line">    User admin</span><br><span class="line">    IdentityFile ~/.ssh/bastion_key</span><br><span class="line"></span><br><span class="line">Host target</span><br><span class="line">    HostName 192.168.10.10</span><br><span class="line">    User alice</span><br><span class="line">    ProxyJump bastion</span><br></pre></td></tr></table></figure><p>直接 <code>ssh target</code> 即可完成两步连接。</p><p><strong>命令行方式：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -J admin@jump.example.com alice@192.168.10.10</span><br></pre></td></tr></table></figure><p>也可以使用多级跳板（用逗号分隔）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh -J user@hop1,user@hop2 user@final</span><br></pre></td></tr></table></figure><p>SSH 内部会自动建立到各跳板机的连接，然后从最后一跳连接最终目标，整个过程对用户透明，并且 <strong>不会自动转发 ssh-agent</strong>，安全性远高于 agent forwarding（见下）。</p><h3 id="SSH-Agent-与转发"><a href="#SSH-Agent-与转发" class="headerlink" title="SSH Agent 与转发"></a>SSH Agent 与转发</h3><p><code>ssh-agent</code> 是一个在后台运行的程序，用于缓存解密的私钥。当你生成密钥并设置了 passphrase，每次使用都需要输入密码很麻烦。把密钥添加到 agent 后，只要 agent 不关闭，后续连接就无需输入 passphrase。</p><p><strong>启动与添加密钥：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 启动 agent（多数桌面环境已自动启动）</span></span><br><span class="line"><span class="built_in">eval</span> <span class="string">&quot;<span class="subst">$(ssh-agent -s)</span>&quot;</span></span><br><span class="line"><span class="comment"># 添加默认密钥，输入一次 passphrase</span></span><br><span class="line">ssh-add ~/.ssh/id_ed25519</span><br><span class="line"><span class="comment"># 列出已加载的密钥</span></span><br><span class="line">ssh-add -l</span><br></pre></td></tr></table></figure><p><strong>Agent 转发（谨慎使用）</strong><br>使用 <code>ssh -A user@server</code> 时，远端的 SSH 客户端可以访问你本地的 ssh-agent，从而用你本机的密钥继续登录其他机器。这在过去经常用来跨跳板机连接内网，但<strong>存在重大安全隐患</strong>：如果远程服务器被攻陷，root 用户能够通过 agent 套接字冒用你的身份。<br><strong>现代最佳实践</strong>：用 <code>ProxyJump</code> 或 <code>ssh -J</code> 替代 agent 转发，它们在本地完成跳转，根本不需要在远程访问 agent。除非万不得已，否则始终关闭 agent 转发（在配置中设置 <code>ForwardAgent no</code>）。</p><p><strong>Windows 上的 ssh-agent</strong>：<br>Windows 提供了一个 <code>ssh-agent</code> 服务，可设置为自动启动，然后在 PowerShell 中用 <code>ssh-add</code> 添加密钥。WSL2 也可以桥接使用该 agent（<code>ssh-agent</code> 在 WSL 中可通过 socket 与 Windows 端互通，环境变量 <code>SSH_AUTH_SOCK</code> 可配置）。</p><h3 id="连接复用（多路复用）"><a href="#连接复用（多路复用）" class="headerlink" title="连接复用（多路复用）"></a>连接复用（多路复用）</h3><p>如果你需要频繁向同一台主机发起 SSH 连接（比如执行命令、传文件、开新会话），每次都进行完整的认证和加密协商会慢且浪费资源。<strong>连接复用</strong>让后续连接共享同一条已认证的 TCP 连接。</p><p>在 <code>~/.ssh/config</code> 中添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Host *</span><br><span class="line">    ControlMaster auto</span><br><span class="line">    ControlPath ~/.ssh/sockets/%r@%h:%p</span><br><span class="line">    ControlPersist 600</span><br></pre></td></tr></table></figure><ul><li><code>ControlMaster auto</code>：自动试用已存在的连接，没有则新建。</li><li><code>ControlPath</code>：指定控制套接字存放路径，<code>%r</code> 远程用户名，<code>%h</code> 主机，<code>%p</code> 端口。</li><li><code>ControlPersist 600</code>：主连接退出后，后台保持该连接 600 秒，供后续复用。</li></ul><p>首次连接后，再次 <code>ssh</code> 或 <code>sftp</code> 同一主机几乎瞬间完成。你可以用以下命令管理主连接：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ssh -O check dev      <span class="comment"># 检查主连接是否存在</span></span><br><span class="line">ssh -O <span class="built_in">exit</span> dev       <span class="comment"># 关闭主连接</span></span><br><span class="line">ssh -O stop dev       <span class="comment"># 停止接受新的复用并关闭（已有会话不受影响）</span></span><br></pre></td></tr></table></figure><h3 id="保持连接不中断"><a href="#保持连接不中断" class="headerlink" title="保持连接不中断"></a>保持连接不中断</h3><p>闲置的 SSH 连接容易被防火墙或 NAT 设备切断。可以在配置文件中加入心跳机制：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Host *</span><br><span class="line">    ServerAliveInterval 60</span><br><span class="line">    ServerAliveCountMax 5</span><br></pre></td></tr></table></figure><p>客户端每 60 秒发送一个加密的空包（心跳），连续 5 次（共 5 分钟）无响应则断开连接。也可以使用 <code>TCPKeepAlive yes</code>，但服务器端心跳可能更可靠。</p><h3 id="远程执行命令"><a href="#远程执行命令" class="headerlink" title="远程执行命令"></a>远程执行命令</h3><p>SSH 可以非交互式地远程执行命令，常用于脚本或自动化。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">ssh user@host <span class="string">&quot;ls -la /var/log&quot;</span></span><br><span class="line"><span class="comment"># 执行多条命令</span></span><br><span class="line">ssh user@host <span class="string">&quot;cd /opt &amp;&amp; ./backup.sh&quot;</span></span><br><span class="line"><span class="comment"># 执行本地脚本（将脚本内容通过 SSH 传递）</span></span><br><span class="line">ssh user@host <span class="string">&#x27;bash -s&#x27;</span> &lt; local_script.sh</span><br><span class="line"><span class="comment"># 运行需要伪终端的命令（如 top、tcpdump），加 -t 参数</span></span><br><span class="line">ssh -t user@host <span class="string">&quot;top&quot;</span></span><br></pre></td></tr></table></figure><p>注意引号的使用：外部用双引号，内部需要保留原样就用单引号，或合理转义。</p><h3 id="挂载远程文件系统（SSHFS）"><a href="#挂载远程文件系统（SSHFS）" class="headerlink" title="挂载远程文件系统（SSHFS）"></a>挂载远程文件系统（SSHFS）</h3><p>SSHFS 基于 FUSE（用户空间文件系统），允许你将远程目录直接挂载到本地，像操作本地硬盘一样读写。</p><p><strong>安装：</strong></p><ul><li>Linux：<code>sudo apt install sshfs</code>（或对应包管理器）</li><li>macOS：<code>brew install macfuse &amp;&amp; brew install sshfs</code></li><li>Windows：安装 WinFsp 和 SSHFS-Win，挂载后会出现盘符。（GUI : sshfs-win-manager）</li></ul><p><strong>使用方法：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建挂载点</span></span><br><span class="line"><span class="built_in">mkdir</span> ~/remote_mount</span><br><span class="line"><span class="comment"># 挂载远程目录</span></span><br><span class="line">sshfs user@host:/remote/path ~/remote_mount</span><br><span class="line"><span class="comment"># 指定端口或附加选项</span></span><br><span class="line">sshfs -p 2222 user@host:/path ~/mnt -o allow_other,reconnect</span><br><span class="line"><span class="comment"># Windows 专用</span></span><br><span class="line">net use Z: \\sshfs.r\user@192.168.1.100\home\user</span><br></pre></td></tr></table></figure><p><strong>卸载：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Linux</span></span><br><span class="line">fusermount -u ~/remote_mount</span><br><span class="line"><span class="comment"># macOS</span></span><br><span class="line">umount ~/remote_mount</span><br><span class="line"><span class="comment"># Windows：在资源管理器中弹出，或 net use 删除网络驱动器</span></span><br></pre></td></tr></table></figure><p>SSHFS 适合临时工作，性能不如 NFS，但在安全性与便捷性之间取得了很好的平衡。</p><h3 id="X11-转发（运行远程图形程序）"><a href="#X11-转发（运行远程图形程序）" class="headerlink" title="X11 转发（运行远程图形程序）"></a>X11 转发（运行远程图形程序）</h3><p>如果你需要在本地显示运行在远程 Linux 上的图形程序（如 gedit、firefox、安装向导等），可以启用 SSH 的 X11 转发。</p><p><strong>服务器端要求</strong>：安装 <code>xauth</code>，并在 <code>/etc/ssh/sshd_config</code> 中确保：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">X11Forwarding yes</span><br><span class="line">X11DisplayOffset 10</span><br></pre></td></tr></table></figure><p>重启 sshd。</p><p><strong>客户端连接：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># -X 启用 X11 转发，-C 压缩流量</span></span><br><span class="line">ssh -XC user@host</span><br><span class="line"><span class="comment"># 登录后运行应用程序</span></span><br><span class="line">xclock &amp;</span><br></pre></td></tr></table></figure><ul><li><strong>macOS 用户</strong> 需安装 XQuartz（<code>brew install --cask xquartz</code>）并启动它。</li><li><strong>Windows 用户</strong> 需安装 X Server 如 VcXsrv 或 Xming，启动后设置 <code>DISPLAY</code> 环境变量（如 <code>export DISPLAY=localhost:0.0</code>，WSL 下可能自动设置）。连接时使用 <code>ssh -Y</code>（可信转发）可解决某些权限问题。</li></ul><hr><h2 id="跨平台注意事项"><a href="#跨平台注意事项" class="headerlink" title="跨平台注意事项"></a>跨平台注意事项</h2><ul><li><strong>权限</strong>：Linux&#x2F;macOS 下 <code>.ssh</code> 目录和密钥的权限必须严格（目录 700，私钥 600）。Windows OpenSSH 对文件权限也有要求，通常只需确保只有自己的用户和 SYSTEM 账户有完全控制，可通过文件属性 → 安全 → 高级 → 更改权限来调整。在 WSL 内访问 Windows 文件系统时，权限继承自 Windows，可能需要单独设置 <code>chmod</code> 但可能不生效（NTFS 无 Unix 权限），建议将私钥存放在 WSL 原生 ext4 文件系统中。</li><li><strong>路径分隔符</strong>：Windows 原生 OpenSSH 支持正斜杠 <code>~/.ssh/config</code>，不必使用反斜杠。</li><li><strong>SSH Agent</strong>：Windows 上可通过服务管理控制台或 PowerShell 设置 <code>ssh-agent</code> 自启动：<figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">Set-Service</span> ssh<span class="literal">-agent</span> <span class="literal">-StartupType</span> Automatic</span><br><span class="line"><span class="built_in">Start-Service</span> ssh<span class="literal">-agent</span></span><br></pre></td></tr></table></figure>之后 <code>ssh-add</code> 添加密钥，WSL 内可通过 <code>wsl-env</code> 等工具共享 agent。</li><li><strong>WSL 互操作</strong>：WSL 中可以创建符号链接将 Windows 的 <code>.ssh</code> 挂接到 Linux 路径，如 <code>ln -s /mnt/c/Users/You/.ssh ~/.ssh</code>，但注意权限问题。更好的方式是在 WSL 内部维护独立的 <code>.ssh</code> 目录，并通过脚本同步。</li><li><strong>终端</strong>：推荐 Windows Terminal，支持多选项卡、GPU 渲染、完整字体，可在 PowerShell、CMD、WSL 会话间无缝切换。</li><li><strong>git 与 SSH</strong>：在 Windows 上使用 git 时，可通过设置环境变量 <code>GIT_SSH_COMMAND</code> 指向原生 <code>ssh.exe</code>，例如 <code>setx GIT_SSH_COMMAND &quot;C:\Windows\System32\OpenSSH\ssh.exe&quot;</code>，避免 git 自带 ssh 引起的混乱。</li></ul><hr><h2 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h2><p>SSH 连接失败时，<strong>添加 <code>-v</code>（verbose）是诊断的第一准则</strong>，可多次叠加（<code>-vvv</code>）获得超详细日志。</p><ol><li><p><strong>Permission denied (publickey)</strong></p><ul><li>确认远程服务器上的公钥已正确添加到 <code>~/.ssh/authorized_keys</code>。</li><li>检查本地私钥路径，可显式使用 <code>-i</code> 指定。</li><li>检查远程权限：<code>chmod 700 ~/.ssh; chmod 600 ~/.ssh/authorized_keys</code>。</li><li>确认 ssh-agent 里有对应的密钥（<code>ssh-add -l</code>）。</li><li>查看服务器日志：<code>sudo tail -f /var/log/auth.log</code>（Debian系）或 <code>/var/log/secure</code>（RHEL系），可能提示具体原因。</li></ul></li><li><p><strong>Host key verification failed</strong></p><ul><li>远程主机指纹与本机记录不符。若确认为正常变更，可清除旧记录：<code>ssh-keygen -R 主机名或IP</code>。</li><li>如果不想检查，可在测试环境临时禁用：<code>ssh -o StrictHostKeyChecking=no user@host</code>（<strong>生产环境切勿使用</strong>）。</li></ul></li><li><p><strong>Connection timed out &#x2F; No route to host</strong></p><ul><li>检查防火墙是否开放 SSH 端口（包括本地防火墙和云服务商安全组）。</li><li>在服务器上确认 sshd 正在监听：<code>ss -tlnp | grep ssh</code>。</li><li>测试网络连通性：<code>ping 主机IP</code>，若 ping 不通则基础设施层存在问题。</li><li>尝试 telnet 端口：<code>telnet host 22</code>，看是否响应 SSH banner。</li></ul></li><li><p><strong>ssh-agent 未运行</strong></p><ul><li>Linux&#x2F;macOS：执行 <code>eval &quot;$(ssh-agent -s)&quot;</code> 然后 <code>ssh-add</code>。</li><li>Windows：确定 ssh-agent 服务已启动，或手动 <code>Start-Service ssh-agent</code>。</li><li>若 agent 转发失败，在远程主机上检查环境变量 <code>SSH_AUTH_SOCK</code> 是否存在。</li></ul></li><li><p><strong>密钥权限太开放</strong></p><ul><li>典型错误：<code>Permissions 0644 for &#39;/home/user/.ssh/id_ed25519&#39; are too open.</code><br>解决：<code>chmod 600 ~/.ssh/id_ed25519</code>。若密钥存放于 NTFS 分区且无法修改权限，可将密钥移入 WSL 的 home 目录，或使用 Windows 属性 -&gt; 安全 -&gt; 高级 -&gt; 禁用继承，仅保留自己的账户。</li></ul></li><li><p><strong>SCP &#x2F; SFTP 卡住或无响应</strong></p><ul><li>可能是 shell 配置文件（如 <code>.bashrc</code>）中输出了文本，干扰了协议。确保远程用户的 shell 启动文件不产生非交互式输出，或仅在交互模式下输出（检查 <code>$-</code> 或 <code>PS1</code>）。</li></ul></li><li><p><strong>连接复用导致的问题</strong></p><ul><li>如果修改了密钥或配置但似乎未生效，可能是旧的复用连接仍在使用旧参数。执行 <code>ssh -O exit 主机名</code> 关闭主连接后重试。</li></ul></li></ol>]]></content>
      
      
      <categories>
          
          <category> Linux教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> SSH </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>班级宠物乐园系统安全漏洞报告</title>
      <link href="/20260401/pet-system-security-report/"/>
      <url>/20260401/pet-system-security-report/</url>
      
        <content type="html"><![CDATA[<p><strong>测试对象</strong>: 生产环境 (http:&#x2F;&#x2F;[数据脱敏]&#x2F;pet&#x2F;)  </p><hr><h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p>本次安全评估发现该系统存在<strong>4个安全漏洞</strong>，且<strong>全部为已验证&#x2F;可利用状态</strong>。</p><hr><h2 id="漏洞详情"><a href="#漏洞详情" class="headerlink" title="漏洞详情"></a>漏洞详情</h2><h3 id="管理员密钥硬编码"><a href="#管理员密钥硬编码" class="headerlink" title="管理员密钥硬编码"></a>管理员密钥硬编码</h3><h4 id="漏洞详情-1"><a href="#漏洞详情-1" class="headerlink" title="漏洞详情"></a>漏洞详情</h4><p>系统在前端JavaScript中硬编码了管理员后台访问密钥，任何用户通过浏览器开发者工具即可获取。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 源码中的硬编码密钥</span></span><br><span class="line"><span class="keyword">const</span> dy = <span class="string">&quot;petclass_admin_2024&quot;</span>;</span><br><span class="line"><span class="keyword">const</span> yn = &#123;<span class="string">&quot;X-Admin-Secret&quot;</span>: dy&#125;;</span><br></pre></td></tr></table></figure><h4 id="验证过程"><a href="#验证过程" class="headerlink" title="验证过程"></a>验证过程</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 获取所有用户信息</span></span><br><span class="line">$ curl -X GET http://[数据脱敏]:2016/api/admin/users \</span><br><span class="line">  -H <span class="string">&quot;X-Admin-Secret: petclass_admin_2024&quot;</span></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 批量生成激活码</span></span><br><span class="line">$ curl -X POST http://[数据脱敏]:2016/api/admin/generate-codes \</span><br><span class="line">  -H <span class="string">&quot;X-Admin-Secret: petclass_admin_2024&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;&quot;count&quot;: 10, &quot;codeType&quot;: &quot;half_year&quot;&#125;&#x27;</span></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 导出所有未使用激活码</span></span><br><span class="line">$ curl -X GET <span class="string">&quot;http://[数据脱敏]:2016/api/admin/codes/export?unused_only=true&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;X-Admin-Secret: petclass_admin_2024&quot;</span></span><br></pre></td></tr></table></figure><hr><h3 id="MySQL-数据库端口暴露"><a href="#MySQL-数据库端口暴露" class="headerlink" title="MySQL 数据库端口暴露"></a>MySQL 数据库端口暴露</h3><h4 id="验证结果"><a href="#验证结果" class="headerlink" title="验证结果"></a>验证结果</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Telnet 是纯文本协议，而握手包是二进制数据，所以把不可打印的字符显示成了乱码</span></span><br><span class="line">$ telnet [数据脱敏] 3306</span><br><span class="line">Trying [数据脱敏]...</span><br><span class="line">Connected to [数据脱敏].</span><br><span class="line">N</span><br><span class="line">5.7.44-logx(h1&#125;xZ�-Pd)^e!sGoAgmysql_native_password</span><br></pre></td></tr></table></figure><hr><h3 id="API接口暴露"><a href="#API接口暴露" class="headerlink" title="API接口暴露"></a>API接口暴露</h3><h4 id="源码证据"><a href="#源码证据" class="headerlink" title="源码证据"></a>源码证据</h4><p>所有后端API接口路径在前端JavaScript中完全暴露：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 完整API接口地图</span></span><br><span class="line"><span class="comment">// 认证接口</span></span><br><span class="line"><span class="variable constant_">POST</span>   /api/auth/login</span><br><span class="line"><span class="variable constant_">POST</span>   /api/auth/register</span><br><span class="line"><span class="variable constant_">POST</span>   /api/auth/change-password</span><br><span class="line"><span class="variable constant_">POST</span>   /api/auth/renew</span><br><span class="line"><span class="variable constant_">GET</span>    /api/auth/check</span><br><span class="line"></span><br><span class="line"><span class="comment">// 数据接口</span></span><br><span class="line"><span class="variable constant_">GET</span>    /api/data/init</span><br><span class="line"><span class="variable constant_">POST</span>   /api/data/students/save</span><br><span class="line"><span class="variable constant_">PUT</span>    /api/data/settings</span><br><span class="line"><span class="variable constant_">DELETE</span> /api/data/student/&#123;id&#125;</span><br><span class="line"><span class="variable constant_">DELETE</span> /api/data/students/by-<span class="keyword">class</span>/&#123;id&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 管理接口</span></span><br><span class="line"><span class="variable constant_">GET</span>    /api/admin/stats</span><br><span class="line"><span class="variable constant_">GET</span>    /api/admin/codes</span><br><span class="line"><span class="variable constant_">GET</span>    /api/admin/users</span><br><span class="line"><span class="variable constant_">POST</span>   /api/admin/generate-codes</span><br><span class="line"><span class="variable constant_">GET</span>    /api/admin/codes/<span class="keyword">export</span></span><br><span class="line"><span class="variable constant_">POST</span>   /api/admin/codes/enable</span><br><span class="line"><span class="variable constant_">POST</span>   /api/admin/codes/disable</span><br></pre></td></tr></table></figure><h4 id="验证：未授权访问测试"><a href="#验证：未授权访问测试" class="headerlink" title="验证：未授权访问测试"></a>验证：未授权访问测试</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 即使没有管理员密钥，接口路径完全暴露，可进行暴力破解</span></span><br><span class="line">$ curl -X GET http://[数据脱敏]:2016/api/admin/users</span><br><span class="line"><span class="comment"># 返回：&#123;&quot;detail&quot;:&quot;admin secret error&quot;&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 但密钥已知，完全可控</span></span><br><span class="line">$ curl -X GET http://[数据脱敏]:2016/api/admin/users \</span><br><span class="line">  -H <span class="string">&quot;X-Admin-Secret: petclass_admin_2024&quot;</span></span><br><span class="line"><span class="comment"># 返回：用户数据</span></span><br></pre></td></tr></table></figure><hr><h3 id="敏感数据明文存储"><a href="#敏感数据明文存储" class="headerlink" title="敏感数据明文存储"></a>敏感数据明文存储</h3><h4 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// localStorage</span></span><br><span class="line"><span class="keyword">const</span> Y = <span class="title function_">G0</span>(<span class="title function_">Z0</span>(<span class="function">(<span class="params">e, t</span>) =&gt;</span> (&#123;</span><br><span class="line">    <span class="attr">user</span>: <span class="literal">null</span>,           <span class="comment">// 用户信息</span></span><br><span class="line">    <span class="attr">token</span>: <span class="literal">null</span>,          <span class="comment">// JWT令牌</span></span><br><span class="line">    <span class="attr">isLoggedIn</span>: <span class="literal">false</span>,</span><br><span class="line">    <span class="attr">students</span>: [],         <span class="comment">// 所有学生数据</span></span><br><span class="line">    <span class="attr">classes</span>: [],          <span class="comment">// 班级数据</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;), &#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&quot;pet-class-storage&quot;</span>,</span><br><span class="line">    <span class="attr">partialize</span>: <span class="function"><span class="params">e</span> =&gt;</span> (&#123;<span class="attr">user</span>: e.<span class="property">user</span>, <span class="attr">token</span>: e.<span class="property">token</span>, <span class="attr">isLoggedIn</span>: e.<span class="property">isLoggedIn</span>&#125;),</span><br><span class="line">    <span class="comment">// 数据直接存储在localStorage</span></span><br><span class="line">&#125;));</span><br></pre></td></tr></table></figure><h4 id="风险验证"><a href="#风险验证" class="headerlink" title="风险验证"></a>风险验证</h4><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 攻击模拟</span></span><br><span class="line">(async function demoAttack() <span class="punctuation">&#123;</span></span><br><span class="line"></span><br><span class="line">    const stolen = JSON.parse(localStorage.getItem(&#x27;pet-class-storage&#x27;));</span><br><span class="line">    const token = stolen.state.token;</span><br><span class="line">    const user = stolen.state.user;</span><br><span class="line"></span><br><span class="line">    console.log(`  Token<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>token<span class="punctuation">&#125;</span>`);</span><br><span class="line">    console.log(`  用户<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>user.username<span class="punctuation">&#125;</span>`);</span><br><span class="line">    console.log(`  套餐<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>user.codeType<span class="punctuation">&#125;</span>`);</span><br><span class="line">    console.log(`  到期<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>user.expiresAt<span class="punctuation">&#125;</span>`);</span><br><span class="line">    </span><br><span class="line">    console.log(&#x27;\n 使用Token访问API...&#x27;);</span><br><span class="line">    </span><br><span class="line">    try <span class="punctuation">&#123;</span></span><br><span class="line">        const response = await fetch(&#x27;http<span class="punctuation">:</span><span class="comment">//[数据脱敏]:2016/api/data/init&#x27;, &#123;</span></span><br><span class="line">            headers<span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">                &#x27;Authorization&#x27;<span class="punctuation">:</span> `Bearer $<span class="punctuation">&#123;</span>token<span class="punctuation">&#125;</span>`<span class="punctuation">,</span></span><br><span class="line">                &#x27;Content-Type&#x27;<span class="punctuation">:</span> &#x27;application/json&#x27;</span><br><span class="line">            <span class="punctuation">&#125;</span></span><br><span class="line">        <span class="punctuation">&#125;</span>);</span><br><span class="line">        </span><br><span class="line">        if (response.ok) <span class="punctuation">&#123;</span></span><br><span class="line">            const data = await response.json();</span><br><span class="line">            console.log(&#x27;  API访问成功！&#x27;);</span><br><span class="line">            console.log(`  班级数<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>data.classes?.length || <span class="number">0</span><span class="punctuation">&#125;</span>`);</span><br><span class="line">            console.log(`  学生数<span class="punctuation">:</span> $<span class="punctuation">&#123;</span>data.students?.length || <span class="number">0</span><span class="punctuation">&#125;</span>`);</span><br><span class="line">            console.log(&#x27;  完整数据<span class="punctuation">:</span>&#x27;<span class="punctuation">,</span> data);</span><br><span class="line">        <span class="punctuation">&#125;</span> else <span class="punctuation">&#123;</span></span><br><span class="line">            console.log(&#x27;API访问失败<span class="punctuation">:</span>&#x27;<span class="punctuation">,</span> response.status);</span><br><span class="line">        <span class="punctuation">&#125;</span></span><br><span class="line">    <span class="punctuation">&#125;</span> catch (error) <span class="punctuation">&#123;</span></span><br><span class="line">        console.log(&#x27;请求失败<span class="punctuation">:</span>&#x27;<span class="punctuation">,</span> error.message);</span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span>)();</span><br></pre></td></tr></table></figure><hr><h3 id="用户可控URL缺乏安全校验"><a href="#用户可控URL缺乏安全校验" class="headerlink" title="用户可控URL缺乏安全校验"></a><del>用户可控URL缺乏安全校验</del></h3><p><strong>经分析，该URL处理逻辑仅在客户端浏览器执行。由于浏览器的同源策略限制，攻击者无法通过此功能读取内网服务响应内容，也无法造成敏感信息泄露。此问题不属于安全漏洞</strong></p><hr><p><em>本报告仅用于安全评估目的，请勿用于非法用途。</em></p>]]></content>
      
      
      <categories>
          
          <category> 安全研究 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 安全审计 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Ventoy Arch To Go 安装指南（VMware + VHD 方案）</title>
      <link href="/20250809/ventoy-arch-linux-portable/"/>
      <url>/20250809/ventoy-arch-linux-portable/</url>
      
        <content type="html"><![CDATA[<h1 id="Ventoy-Arch-To-Go-安装指南"><a href="#Ventoy-Arch-To-Go-安装指南" class="headerlink" title="Ventoy Arch To Go 安装指南"></a><strong>Ventoy</strong> Arch To Go 安装指南</h1><blockquote><p>Arch殿堂之上，历代发行版纷争百余次，是非功过未易评断，然技术史家无不承认，正是在这方终端之中，决定了多少开发者的心智归属、系统生态的流向沉浮，故而社区素有“真理之殿”的传说。</p><p>当年林纳斯托瓦兹首释内核源代码，开源运动如星火燎原。Debian以稳如磐石之志立下基石，红帽借企业之力拓土开疆。公元二〇〇三年，也正是在这滚动更新的浪潮前，Arch社区以KISS之道铸就极简利刃，以Wiki为盾，以AUR为弓，于纷繁发行版中夺下一片天地！</p><p>我不明白，为何众人总在议论 Debian 的稳如磐石，仿佛这自由世界对我们注定了曲高和寡。数年前，我从一份纯净的安装媒介踏上征途，开启了滚动发布的新章，Linux桌面的灵活精悍遂入新境。Pacman所到之处，极客欣然相迎，真可谓尽握自主，那种清晰透明、万物由我的境界，犹在眼前。短短数载之后，这里竟至于一变而成为我等理念的困守之地了么？</p><p>无论怎么讲，版本迭代，是滚动对固定，优势在我！</p></blockquote><h2 id="准备工作（VMware-Workstation-Pro-VHD-固定磁盘）"><a href="#准备工作（VMware-Workstation-Pro-VHD-固定磁盘）" class="headerlink" title="准备工作（VMware Workstation Pro + VHD 固定磁盘）"></a>准备工作（VMware Workstation Pro + VHD 固定磁盘）</h2><p>如果你计划在 VMware Workstation Pro 中制作 Arch To Go，并使用固定大小的 VHD 虚拟磁盘作为系统盘，请按照以下步骤准备：</p><h3 id="1-创建-VHD-固定磁盘（Windows-环境）"><a href="#1-创建-VHD-固定磁盘（Windows-环境）" class="headerlink" title="1. 创建 VHD 固定磁盘（Windows 环境）"></a>1. 创建 VHD 固定磁盘（Windows 环境）</h3><ol><li>打开“磁盘管理”（Win + X → 磁盘管理）</li><li>选择“操作” → “创建 VHD”</li><li>指定保存路径（如 <code>D:\ArchLinux.vhd</code>）</li><li>设置虚拟硬盘大小（建议至少 <strong>30 GB</strong>）</li><li>**选择“固定大小” ** <strong>（必须）</strong></li><li>确认创建，磁盘管理会自动挂载此 VHD</li></ol><h3 id="2-初始化-VHD-分区（可选但推荐）"><a href="#2-初始化-VHD-分区（可选但推荐）" class="headerlink" title="2. 初始化 VHD 分区（可选但推荐）"></a>2. 初始化 VHD 分区（可选但推荐）</h3><p>在磁盘管理中：</p><ul><li>找到新添加的 VHD 磁盘，右键选择“初始化磁盘”</li><li>选择 <strong>GPT（GUID 分区表）</strong>，确认</li><li>之后可在 Arch 安装过程中再进行分区格式化</li></ul><h3 id="3-配置-VMware-Workstation-Pro"><a href="#3-配置-VMware-Workstation-Pro" class="headerlink" title="3. 配置 VMware Workstation Pro"></a>3. 配置 VMware Workstation Pro</h3><ol><li>创建新的虚拟机：<ul><li>选择“自定义（高级）”</li><li>虚拟机硬件兼容性默认</li><li>稍后安装操作系统</li><li>选择“Linux” </li><li>虚拟机名称与位置按需设置</li></ul></li><li>处理器与内存：<ul><li>处理器建议 2 核心以上</li><li>内存建议 4 GB 以上</li></ul></li><li>网络类型：<ul><li>选择“桥接模式”或“NAT”均可</li></ul></li><li>I&#x2F;O 控制器与磁盘：<ul><li>选择 SCSI 控制器（推荐）</li><li>选择“使用现有虚拟磁盘”</li><li>浏览并选择之前创建的 VHD 文件（<code>ArchLinux.vhd</code>）</li></ul></li></ol><h3 id="4-加载-Arch-Linux-安装介质"><a href="#4-加载-Arch-Linux-安装介质" class="headerlink" title="4. 加载 Arch Linux 安装介质"></a>4. 加载 Arch Linux 安装介质</h3><ol><li>在虚拟机设置中，添加一个 <strong>CD&#x2F;DVD 驱动器</strong></li><li>选择 Arch Linux 的 ISO 镜像文件（如 <code>archlinux-xxxx.xx.xx-x86_64.iso</code>）</li><li>设置启动顺序，确保 CD&#x2F;DVD 为第一启动项</li></ol><p>接下来，你可以按照本指南的后续步骤进行安装，注意在分区时选择对应的 VHD 设备。</p><hr><h2 id="警告"><a href="#警告" class="headerlink" title="警告"></a>警告</h2><p><strong>部分操作会修改硬盘分区，可能导致数据丢失！</strong><br><strong>请务必备份所有重要数据！</strong><br><strong>请确保你已理解每一步操作的含义，尤其是在分区和格式化阶段。</strong><br><strong>安装前建议查阅 <a href="https://wiki.archlinux.org/">Arch Wiki</a> 获取最新信息。</strong>  </p><span id="more"></span><hr><h2 id="1-验证启动模式"><a href="#1-验证启动模式" class="headerlink" title="1. 验证启动模式"></a>1. 验证启动模式</h2><p>检查系统是否以 UEFI 模式启动（现代电脑基本都是UEFI启动）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ls</span> /sys/firmware/efi/efivars</span><br></pre></td></tr></table></figure><p>如果能看到文件列表，说明是UEFI启动模式。</p><hr><h2 id="2-建立网络连接"><a href="#2-建立网络连接" class="headerlink" title="2. 建立网络连接"></a>2. 建立网络连接</h2><p>启用网络并测试连通性：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">dhcpcd          <span class="comment"># 自动获取IP地址</span></span><br><span class="line">ping archlinux.org -c 3  <span class="comment"># 测试网络是否通畅</span></span><br></pre></td></tr></table></figure><hr><h2 id="3-同步系统时间"><a href="#3-同步系统时间" class="headerlink" title="3. 同步系统时间"></a>3. 同步系统时间</h2><p>确保系统时间准确（防止安装过程中出现时间错误）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">timedatectl set-ntp <span class="literal">true</span>  <span class="comment"># 启用网络时间同步</span></span><br><span class="line">timedatectl status        <span class="comment"># 检查时间状态</span></span><br></pre></td></tr></table></figure><hr><h2 id="4-磁盘分区与格式化"><a href="#4-磁盘分区与格式化" class="headerlink" title="4. 磁盘分区与格式化"></a>4. 磁盘分区与格式化</h2><h3 id="查看存储设备"><a href="#查看存储设备" class="headerlink" title="查看存储设备"></a>查看存储设备</h3><p>先确认你的磁盘设备名称：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lsblk  <span class="comment"># 列出所有存储设备</span></span><br></pre></td></tr></table></figure><h3 id="分区操作（UEFI-GPT）"><a href="#分区操作（UEFI-GPT）" class="headerlink" title="分区操作（UEFI + GPT）"></a>分区操作（UEFI + GPT）</h3><p> <strong>分区操作具有风险！请务必确认目标设备！</strong><br> <strong>以下示例使用 <code>/dev/nvme0n1</code>，请根据 <code>lsblk</code> 结果替换为你的实际设备！</strong></p><p>使用 <code>gdisk</code> 工具进行分区（比 <code>fdisk</code> 更适合 GPT 分区表）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gdisk /dev/nvme0n1  <span class="comment"># NVMe 固态硬盘（请替换为你的设备）</span></span><br></pre></td></tr></table></figure><p><strong>分区步骤详解：</strong></p><ol><li>进入 <code>gdisk</code> 后，输入 <code>o</code> 创建新的 GPT 分区表 <strong>（仅适用于全新磁盘）</strong></li><li>创建 EFI 分区：<ul><li>输入 <code>n</code> 新建分区</li><li>分区号默认回车</li><li>起始扇区默认回车</li><li>输入 <code>+512M</code> 设置分区大小</li><li>输入 <code>ef00</code> 设置分区类型为 EFI 系统分区</li></ul></li><li>创建交换分区（推荐）：<ul><li>再次输入 <code>n</code> 新建分区</li><li>分区号默认回车</li><li>起始扇区默认回车</li><li>输入 <code>+4G</code> 设置交换分区大小（建议为内存的 1-2 倍，例如 4G、8G）</li><li>输入 <code>8200</code> 设置分区类型为 Linux swap</li></ul></li><li>创建根分区：<ul><li>再次输入 <code>n</code> 新建分区</li><li>分区号默认回车</li><li>起始扇区默认回车</li><li>直接回车使用剩余所有空间（也可指定大小如 <code>+100G</code>）</li><li>分区类型保持默认的 Linux 文件系统 (8300)</li></ul></li><li>输入 <code>w</code> 保存并退出（所有更改将生效）</li></ol><h3 id="验证分区"><a href="#验证分区" class="headerlink" title="验证分区"></a>验证分区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fdisk -l  <span class="comment"># 查看分区结果</span></span><br></pre></td></tr></table></figure><hr><h3 id="格式化分区（二选一：传统-ext4-或-现代-Btrfs）"><a href="#格式化分区（二选一：传统-ext4-或-现代-Btrfs）" class="headerlink" title="格式化分区（二选一：传统 ext4 或 现代 Btrfs）"></a>格式化分区（<strong>二选一：传统 ext4 或 现代 Btrfs</strong>）</h3><p><strong>格式化会永久清除分区内所有数据！</strong></p><h4 id="选项-A：使用-ext4（经典稳定）"><a href="#选项-A：使用-ext4（经典稳定）" class="headerlink" title="选项 A：使用 ext4（经典稳定）"></a>选项 A：使用 ext4（经典稳定）</h4><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mkfs.fat -F32 /dev/nvme0n1p1      <span class="comment"># 格式化 EFI 分区为 FAT32</span></span><br><span class="line">mkfs.ext4 /dev/nvme0n1p3           <span class="comment"># 格式化根分区为 ext4（注意分区号对应）</span></span><br><span class="line">mkswap /dev/nvme0n1p2              <span class="comment"># 初始化交换分区</span></span><br></pre></td></tr></table></figure><h4 id="选项-B：使用-Btrfs（支持快照、压缩、子卷等高级功能）"><a href="#选项-B：使用-Btrfs（支持快照、压缩、子卷等高级功能）" class="headerlink" title="选项 B：使用 Btrfs（支持快照、压缩、子卷等高级功能）"></a>选项 B：使用 Btrfs（支持快照、压缩、子卷等高级功能）</h4><p>Btrfs 是现代 Linux 文件系统，特别适合需要灵活管理、定期备份的场景。配合 VHD 虚拟磁盘使用同样出色。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mkfs.fat -F32 /dev/nvme0n1p1        <span class="comment"># EFI 分区保持 FAT32</span></span><br><span class="line">mkswap /dev/nvme0n1p2                <span class="comment"># 交换分区初始化（与 ext4 相同）</span></span><br><span class="line">mkfs.btrfs -f /dev/nvme0n1p3         <span class="comment"># 格式化根分区为 Btrfs</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>Btrfs 子卷规划</strong>：我们将创建独立的子卷来组织系统，便于快照和回滚。典型的布局包括：</p><ul><li><code>@</code>      ：挂载到 <code>/</code></li><li><code>@home</code>  ：挂载到 <code>/home</code></li><li><code>@cache</code> ：挂载到 <code>/var/cache</code>（可选，用于排除缓存快照）</li><li><code>@log</code>   ：挂载到 <code>/var/log</code>（可选）<br>这里我们创建基本的 <code>@</code> 和 <code>@home</code>。</li></ul></blockquote><p>创建并挂载 Btrfs 子卷（在格式化后立即进行）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 先挂载根分区到临时目录</span></span><br><span class="line">mount /dev/nvme0n1p3 /mnt</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建子卷</span></span><br><span class="line">btrfs subvolume create /mnt/@</span><br><span class="line">btrfs subvolume create /mnt/@home</span><br><span class="line"></span><br><span class="line"><span class="comment"># 卸载根分区，准备按子卷挂载</span></span><br><span class="line">umount /mnt</span><br></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：交换分区不需要 Btrfs 特殊处理，保持独立分区即可。Btrfs 上也可以使用交换文件，但配置较复杂且性能不如独立交换分区，故本指南仍采用独立交换分区。</p></blockquote><hr><h3 id="启用交换分区"><a href="#启用交换分区" class="headerlink" title="启用交换分区"></a>启用交换分区</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">swapon /dev/nvme0n1p2  <span class="comment"># 启用交换分区</span></span><br></pre></td></tr></table></figure><hr><h2 id="5-挂载文件系统"><a href="#5-挂载文件系统" class="headerlink" title="5. 挂载文件系统"></a>5. 挂载文件系统</h2><p>根据你选择的文件系统，挂载方式有所不同。</p><h3 id="如果使用-ext4"><a href="#如果使用-ext4" class="headerlink" title="如果使用 ext4"></a>如果使用 ext4</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mount /dev/nvme0n1p3 /mnt       <span class="comment"># 挂载根分区</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /mnt/boot              <span class="comment"># 创建 boot 目录</span></span><br><span class="line">mount /dev/nvme0n1p1 /mnt/boot  <span class="comment"># 挂载 EFI 分区</span></span><br></pre></td></tr></table></figure><h3 id="如果使用-Btrfs（子卷方式）"><a href="#如果使用-Btrfs（子卷方式）" class="headerlink" title="如果使用 Btrfs（子卷方式）"></a>如果使用 Btrfs（子卷方式）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 挂载根分区（Btrfs 卷）到 /mnt，并使用子卷 @ 作为根</span></span><br><span class="line">mount -o subvol=@ /dev/nvme0n1p3 /mnt</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建必要目录</span></span><br><span class="line"><span class="built_in">mkdir</span> -p /mnt/&#123;boot,home&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 挂载 EFI 分区</span></span><br><span class="line">mount /dev/nvme0n1p1 /mnt/boot</span><br><span class="line"></span><br><span class="line"><span class="comment"># 挂载 @home 子卷到 /mnt/home</span></span><br><span class="line">mount -o subvol=@home /dev/nvme0n1p3 /mnt/home</span><br></pre></td></tr></table></figure><blockquote><p><strong>可选</strong>：若你还创建了其他子卷（如 @cache、@log），也在此一并挂载。</p></blockquote><hr><h2 id="6-配置软件源（国内用户必看）"><a href="#6-配置软件源（国内用户必看）" class="headerlink" title="6. 配置软件源（国内用户必看）"></a>6. 配置软件源（国内用户必看）</h2><p>替换为国内镜像源加速下载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure><p>在文件最前面添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Server = https://mirror.iscas.ac.cn/archlinux/$repo/os/$arch</span><br><span class="line">Server = https://mirrors.ustc.edu.cn/archlinux/$repo/os/$arch</span><br></pre></td></tr></table></figure><hr><h2 id="7-安装基础系统"><a href="#7-安装基础系统" class="headerlink" title="7. 安装基础系统"></a>7. 安装基础系统</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacstrap /mnt base base-devel linux linux-firmware vim dhcpcd</span><br></pre></td></tr></table></figure><blockquote><p>这个命令会安装最基本的系统组件，大约需要 5-15 分钟，取决于你的网速。</p></blockquote><hr><h2 id="8-生成文件系统表"><a href="#8-生成文件系统表" class="headerlink" title="8. 生成文件系统表"></a>8. 生成文件系统表</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</span><br></pre></td></tr></table></figure><p><strong>注意</strong>：如果使用 Btrfs，生成的 fstab 会自动包含子卷挂载选项。建议安装完成后检查 <code>/mnt/etc/fstab</code>，确保子卷挂载正确（例如 <code>subvol=@</code> 等选项存在）。</p><hr><h2 id="9-切换根环境"><a href="#9-切换根环境" class="headerlink" title="9. 切换根环境"></a>9. 切换根环境</h2><blockquote><p><code>arch-chroot</code> 是 Arch Linux 提供的工具，相比标准的 <code>chroot</code>，它会自动挂载 <code>/proc</code>、<code>/sys</code>、<code>/dev</code> 等虚拟文件系统，简化了操作。</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arch-chroot /mnt</span><br></pre></td></tr></table></figure><hr><h2 id="10-配置时区"><a href="#10-配置时区" class="headerlink" title="10. 配置时区"></a>10. 配置时区</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">ln</span> -sf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime</span><br><span class="line">hwclock --systohc</span><br></pre></td></tr></table></figure><hr><h2 id="11-本地化设置"><a href="#11-本地化设置" class="headerlink" title="11. 本地化设置"></a>11. 本地化设置</h2><p>编辑语言配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/locale.gen</span><br></pre></td></tr></table></figure><p>取消注释：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">en_US.UTF-8 UTF-8</span><br><span class="line">zh_CN.UTF-8 UTF-8</span><br></pre></td></tr></table></figure><p>生成本地化设置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">locale-gen</span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;LANG=en_US.UTF-8&quot;</span> &gt; /etc/locale.conf</span><br></pre></td></tr></table></figure><hr><h2 id="12-网络配置"><a href="#12-网络配置" class="headerlink" title="12. 网络配置"></a>12. 网络配置</h2><p>设置主机名：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;archlinux&quot;</span> &gt; /etc/hostname</span><br></pre></td></tr></table></figure><p>编辑 hosts 文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/hosts</span><br></pre></td></tr></table></figure><p>添加内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1   localhost</span><br><span class="line">::1         localhost</span><br><span class="line">127.0.1.1   archlinux.localdomain archlinux</span><br></pre></td></tr></table></figure><hr><h2 id="13-设置-root-密码"><a href="#13-设置-root-密码" class="headerlink" title="13. 设置 root 密码"></a>13. 设置 root 密码</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">passwd</span><br></pre></td></tr></table></figure><hr><h2 id="14-安装引导程序-GRUB"><a href="#14-安装引导程序-GRUB" class="headerlink" title="14. 安装引导程序 (GRUB)"></a>14. 安装引导程序 (GRUB)</h2><p>无论使用 ext4 还是 Btrfs，GRUB 都能良好支持。但 Btrfs 用户需确保 <code>grub</code> 包包含 btrfs 模块（默认已包含）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pacman -S --needed grub efibootmgr</span><br><span class="line">grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=GRUB --removable</span><br><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><hr><h2 id="15-启用网络服务"><a href="#15-启用网络服务" class="headerlink" title="15. 启用网络服务"></a>15. 启用网络服务</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> dhcpcd</span><br></pre></td></tr></table></figure><hr><h2 id="16-创建标准用户"><a href="#16-创建标准用户" class="headerlink" title="16. 创建标准用户"></a>16. 创建标准用户</h2><p>添加用户并设置密码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">useradd -m -G wheel -s /bin/bash username</span><br><span class="line">passwd username</span><br></pre></td></tr></table></figure><p>配置 sudo 权限：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/sudoers</span><br></pre></td></tr></table></figure><p>取消注释：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">%wheel ALL=(ALL:ALL) ALL</span><br></pre></td></tr></table></figure><hr><h2 id="17-启用额外软件源"><a href="#17-启用额外软件源" class="headerlink" title="17. 启用额外软件源"></a>17. 启用额外软件源</h2><p>编辑 pacman 配置文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/pacman.conf</span><br></pre></td></tr></table></figure><p>取消注释启用 multilib：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">[multilib]</span><br><span class="line">Include = /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure><p>添加 Arch Linux CN 源：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">[archlinuxcn]</span><br><span class="line">Server = https://mirrors.ustc.edu.cn/archlinuxcn/$arch</span><br></pre></td></tr></table></figure><p>更新并安装密钥环：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pacman -Sy</span><br><span class="line">pacman -S archlinuxcn-keyring</span><br></pre></td></tr></table></figure><hr><h2 id="18-完成安装"><a href="#18-完成安装" class="headerlink" title="18. 完成安装"></a>18. 完成安装</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">exit</span></span><br><span class="line">umount -R /mnt</span><br><span class="line">reboot</span><br></pre></td></tr></table></figure><hr><h2 id="19-配置默认编辑器"><a href="#19-配置默认编辑器" class="headerlink" title="19. 配置默认编辑器"></a>19. 配置默认编辑器</h2><h3 id="root-账户"><a href="#root-账户" class="headerlink" title="root 账户"></a>root 账户</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim ~/.bash_profile</span><br></pre></td></tr></table></figure><p>添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">export EDITOR=&#x27;vim&#x27;</span><br></pre></td></tr></table></figure><h3 id="普通账户"><a href="#普通账户" class="headerlink" title="普通账户"></a>普通账户</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim ~/.bashrc</span><br></pre></td></tr></table></figure><p>添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">export EDITOR=&#x27;vim&#x27;</span><br></pre></td></tr></table></figure><hr><h2 id="20-安装-KDE-Plasma-桌面环境"><a href="#20-安装-KDE-Plasma-桌面环境" class="headerlink" title="20. 安装 KDE Plasma 桌面环境"></a>20. 安装 KDE Plasma 桌面环境</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacman -S plasma-meta konsole dolphin dolphin-plugins plasma-x11-session plasma-login-manager</span><br></pre></td></tr></table></figure><blockquote><p>注意：不再安装 SDDM，我们将使用 PLM (Plasma Login Manager) 替代。</p></blockquote><hr><h2 id="21-安装基础软件包"><a href="#21-安装基础软件包" class="headerlink" title="21. 安装基础软件包"></a>21. 安装基础软件包</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">pacman -S --needed curl wget nano vim p7zip <span class="built_in">which</span> lvm2</span><br><span class="line"><span class="built_in">sudo</span> pacman -S sof-firmware alsa-firmware alsa-ucm-conf</span><br><span class="line"><span class="built_in">sudo</span> pacman -S ntfs-3g</span><br><span class="line"><span class="built_in">sudo</span> pacman -S adobe-source-han-serif-cn-fonts ttf-liberation</span><br><span class="line"><span class="built_in">sudo</span> pacman -S noto-fonts noto-fonts-cjk noto-fonts-emoji noto-fonts-extra</span><br><span class="line"><span class="built_in">sudo</span> pacman -S firefox chromium</span><br><span class="line"><span class="built_in">sudo</span> pacman -S packagekit-qt6 packagekit appstream-qt appstream</span><br><span class="line"><span class="built_in">sudo</span> pacman -S paru</span><br></pre></td></tr></table></figure><hr><h2 id="22-配置-PLM-Plasma-Login-Manager"><a href="#22-配置-PLM-Plasma-Login-Manager" class="headerlink" title="22. 配置 PLM (Plasma Login Manager)"></a>22. 配置 PLM (Plasma Login Manager)</h2><blockquote><p><strong>PLM</strong> 是基于 KDE Plasma 组件开发的轻量登录管理器，与 Plasma 桌面深度集成，主题一致性好，资源占用更低。</p></blockquote><h3 id="22-1-启用-PLM"><a href="#22-1-启用-PLM" class="headerlink" title="22.1 启用 PLM"></a>22.1 启用 PLM</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> plasmalogin</span><br></pre></td></tr></table></figure><h3 id="22-2-PLM-汉化"><a href="#22-2-PLM-汉化" class="headerlink" title="22.2 PLM 汉化"></a>22.2 PLM 汉化</h3><p><strong>删除用户缓存配置</strong></p><p>PLM 会以 <code>plasmalogin</code> 用户运行，该用户已有旧的区域配置缓存，需要清除：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">rm</span> -f /var/lib/plasmalogin/.config/plasma-localerc</span><br></pre></td></tr></table></figure><blockquote><p>如果不执行此步，PLM 界面可能仍显示英文。</p></blockquote><h3 id="22-3-启动-PLM"><a href="#22-3-启动-PLM" class="headerlink" title="22.3 启动 PLM"></a>22.3 启动 PLM</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl start plasmalogin</span><br></pre></td></tr></table></figure><hr><h2 id="23-启动网络工具"><a href="#23-启动网络工具" class="headerlink" title="23. 启动网络工具"></a>23. 启动网络工具</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">systemctl start NetworkManager</span><br><span class="line">systemctl <span class="built_in">enable</span> NetworkManager</span><br></pre></td></tr></table></figure><blockquote><p>若需要同时使用 dhcpcd 和 NetworkManager，建议只启用 NetworkManager 即可，两者可能冲突。</p></blockquote><hr><h2 id="24-配置中文显示"><a href="#24-配置中文显示" class="headerlink" title="24. 配置中文显示"></a>24. 配置中文显示</h2><h3 id="KDE-语言设置"><a href="#KDE-语言设置" class="headerlink" title="KDE 语言设置"></a>KDE 语言设置</h3><ol><li>打开 <code>Region and Language</code></li><li>点击 <code>Language</code> 后的 <code>Modify</code></li><li>右下角 <code>Add More</code> 添加 <code>简体中文</code></li><li>将中文移至顶部</li></ol><h3 id="确保-PLM-登录界面中文"><a href="#确保-PLM-登录界面中文" class="headerlink" title="确保 PLM 登录界面中文"></a>确保 PLM 登录界面中文</h3><p>如果按照 22.3 节的步骤配置，PLM 登录界面应已显示中文。若仍未生效，检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /var/lib/plasmalogin/.config/plasma-localerc</span><br></pre></td></tr></table></figure><p>确认 <code>LANG=zh_CN.UTF-8</code> 已正确设置，并确保已执行删除缓存的命令。</p><hr><h2 id="25-安装中文输入法"><a href="#25-安装中文输入法" class="headerlink" title="25. 安装中文输入法"></a>25. 安装中文输入法</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> pacman -S fcitx5-im fcitx5-chinese-addons fcitx5-material-color</span><br></pre></td></tr></table></figure><p>配置环境变量：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> vim /etc/environment</span><br></pre></td></tr></table></figure><p>添加：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># GTK_IM_MODULE=fcitx</span><br><span class="line"># QT_IM_MODULE=wayland;fcitx</span><br><span class="line">XMODIFIERS=@im=fcitx</span><br></pre></td></tr></table></figure><hr><h2 id="26-支持-Ventoy-Arch-to-Go（可选）"><a href="#26-支持-Ventoy-Arch-to-Go（可选）" class="headerlink" title="26. 支持 Ventoy Arch to Go（可选）"></a>26. 支持 Ventoy Arch to Go（可选）</h2><blockquote><p>适用于将 Arch Linux 安装到可移动设备（如 U 盘）并配合 Ventoy 使用。</p></blockquote><p>执行官方 <a href="https://github.com/ventoy/vtoyboot/releases">vtoyboot 脚本</a> ，然后将 VHD 文件后缀从 <code>.vhd</code> 修改为 <code>.vtoy</code>，以便 Ventoy 识别并启动。</p><ol><li>下载适合的 vtoyboot 脚本（如 <code>vtoyboot-1.0.24.tar.gz</code>）并解压。</li><li>在虚拟机中运行（或挂载 VHD 到运行的 Linux 系统）：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> ./vtoyboot.sh</span><br></pre></td></tr></table></figure></li><li>脚本会修改 initramfs 以支持从 Ventoy 启动。</li><li>完成后，将 <code>.vhd</code> 文件重命名为 <code>.vtoy</code>（例如 <code>ArchLinux.vtoy</code>），放入 Ventoy 优盘即可。</li></ol><blockquote><p><strong>注意</strong>：vtoyboot 脚本通常需要在内核更新后重新运行，以确保 initramfs 始终包含所需模块。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> Linux教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Arch Linux </tag>
            
            <tag> Ventoy </tag>
            
            <tag> VHD </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>扫雷破解版</title>
      <link href="/20250504/minesweeper-hack/"/>
      <url>/20250504/minesweeper-hack/</url>
      
        <content type="html"><![CDATA[<p>我对经典的扫雷游戏进行了一点修改，现在可以一键显示所有地雷的位置，并轻松实现一键通关！</p><p><img src="https://cdn.jsdelivr.net/gh/ZXCLF/blog_pic/pic1.png" alt="pic1"></p><p>下载： <a href="https://www.123684.com/s/FGWpTd-I4EPh">https://www.123684.com/s/FGWpTd-I4EPh</a></p><h3 id="适用系统："><a href="#适用系统：" class="headerlink" title="适用系统："></a>适用系统：</h3><p>适用于Windows 7及以上的操作系统。</p><h3 id="文件说明："><a href="#文件说明：" class="headerlink" title="文件说明："></a>文件说明：</h3><ul><li>Minesweeper文件夹：原版扫雷游戏。</li><li>Minesweeper_Patched文件夹：破解的扫雷游戏。</li></ul>]]></content>
      
      
      <categories>
          
          <category> 游戏娱乐 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 扫雷 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>NoMoreBugCheck.sys 数字签名版</title>
      <link href="/20250411/nomorebugcheck-driver/"/>
      <url>/20250411/nomorebugcheck-driver/</url>
      
        <content type="html"><![CDATA[<p>下载：<a href="https://www.123684.com/s/FGWpTd-4iQPh">123网盘</a></p><p><strong>虽然数字签名已经过期，但可以正常加载驱动（关闭测试模式）</strong></p><p>经测试 Windows 11 24H2 及以下可正常加载驱动（关闭测试模式）</p>]]></content>
      
      
      <categories>
          
          <category> 系统教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Windows驱动 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>本站说明</title>
      <link href="/20250408/blog-mirror-info/"/>
      <url>/20250408/blog-mirror-info/</url>
      
        <content type="html"><![CDATA[<p>本站主站： <a href="https://zxclf.us.ci/">https://zxclf.us.ci/</a> (使用 Cloudflare 搭建)</p><p>本站镜像站： <a href="https://zxclf.github.io/">https://zxclf.github.io/</a> (使用 Github Pages 搭建)</p><p>本站开源地址：<a href="https://github.com/ZXCLF/ZXCLF.github.io">https://github.com/ZXCLF/ZXCLF.github.io</a></p><p><span class="heimu" title="你知道的太多了">本站由 Hexo 与玄学驱动</span></p><p>今天是：  <span class="live-date" id="hexo-live-date-1787843352407-t5oeai"  data-mode="today"  data-date=""  data-format="YYYY.MM.DD"  data-yearly="false"  data-include-start="false">加载中...</span>  <script>  (function() {    if (!window.__liveDate) {      window.__liveDate = {        dayjsReady: null,        initCalled: false      };    }    const liveDate = window.__liveDate;    function ensureDayjs() {      if (typeof dayjs !== 'undefined') {        return Promise.resolve();      }      if (!liveDate.dayjsReady) {        liveDate.dayjsReady = new Promise((resolve, reject) => {          const script = document.createElement('script');          script.src = '/js/dayjs.min.js';          script.onload = () => resolve();          script.onerror = () => reject(new Error('Failed to load dayjs'));          document.head.appendChild(script);        });      }      return liveDate.dayjsReady;    }    function updateElement(el) {      try {        const mode = el.dataset.mode;        const date = el.dataset.date;        const format = el.dataset.format;        const yearly = el.dataset.yearly === 'true';        const includeStart = el.dataset.includeStart === 'true';        const now = dayjs();        const target = dayjs(date);        let value = '';        if (mode === 'today') {          value = now.format(format);        } else if (target.isValid()) {          if (mode === 'format') {            let d = target;            if (yearly) {              const today = dayjs().startOf('day');              d = target.startOf('day');              while (d.isBefore(today)) d = d.add(1, 'year');            }            value = d.format(format);          } else if (mode === 'days-since') {            const diff = now.diff(target, 'day', true);            value = diff >= 0 ? String(Math.floor(diff) + 1) : '0';          } else if (mode === 'days-until') {            let d = target;            if (yearly) {              const today = dayjs().startOf('day');              d = target.startOf('day');              while (d.isBefore(today)) d = d.add(1, 'year');            }            value = String(d.startOf('day').diff(now.startOf('day'), 'day'));          } else if (mode === 'days-gap') {            const diff = now.diff(target, 'day', true);            value = diff >= 0            ? String(Math.floor(diff) + (includeStart ? 1 : 0))            : String(Math.ceil(-diff));          } else if (mode === 'age') {            value = String(now.diff(target, 'year'));          }        }        if (value !== '') {          el.textContent = value;        }      } catch (error) {        console.error('[LiveDate] Update error:', error);        el.textContent = '日期格式错误';      }    }    function initElements() {      const elements = document.querySelectorAll('.live-date[data-mode]:not([data-initialized])');      elements.forEach(el => {        el.dataset.initialized = 'true';        updateElement(el);        // 每分钟更新一次        setInterval(() => updateElement(el), 60000);      });    }    function startInit() {      ensureDayjs()      .then(() => {        initElements();      })      .catch(error => {        console.error('[LiveDate]', error);        document.querySelectorAll('.live-date[data-mode]:not([data-initialized])').forEach(el => {          el.textContent = '加载失败';          el.dataset.initialized = 'true';        });      });    }    if (document.readyState === 'loading') {      document.addEventListener('DOMContentLoaded', startInit);    } else {      startInit();    }    // 暴露全局重置函数    if (!window.__resetLiveDate) {      window.__resetLiveDate = function() {        document.querySelectorAll('.live-date[data-initialized]').forEach(el => {          delete el.dataset.initialized;        });        if (typeof dayjs !== 'undefined') {          initElements();        }      };    }  })();  </script>  </p><p>本站已运行：  <span class="live-date" id="hexo-live-date-1787843352407-37owfq"  data-mode="days-since"  data-date="2025-01-17"  data-format="YYYY.MM.DD"  data-yearly="false"  data-include-start="false">加载中...</span>  <script>  (function() {    if (!window.__liveDate) {      window.__liveDate = {        dayjsReady: null,        initCalled: false      };    }    const liveDate = window.__liveDate;    function ensureDayjs() {      if (typeof dayjs !== 'undefined') {        return Promise.resolve();      }      if (!liveDate.dayjsReady) {        liveDate.dayjsReady = new Promise((resolve, reject) => {          const script = document.createElement('script');          script.src = '/js/dayjs.min.js';          script.onload = () => resolve();          script.onerror = () => reject(new Error('Failed to load dayjs'));          document.head.appendChild(script);        });      }      return liveDate.dayjsReady;    }    function updateElement(el) {      try {        const mode = el.dataset.mode;        const date = el.dataset.date;        const format = el.dataset.format;        const yearly = el.dataset.yearly === 'true';        const includeStart = el.dataset.includeStart === 'true';        const now = dayjs();        const target = dayjs(date);        let value = '';        if (mode === 'today') {          value = now.format(format);        } else if (target.isValid()) {          if (mode === 'format') {            let d = target;            if (yearly) {              const today = dayjs().startOf('day');              d = target.startOf('day');              while (d.isBefore(today)) d = d.add(1, 'year');            }            value = d.format(format);          } else if (mode === 'days-since') {            const diff = now.diff(target, 'day', true);            value = diff >= 0 ? String(Math.floor(diff) + 1) : '0';          } else if (mode === 'days-until') {            let d = target;            if (yearly) {              const today = dayjs().startOf('day');              d = target.startOf('day');              while (d.isBefore(today)) d = d.add(1, 'year');            }            value = String(d.startOf('day').diff(now.startOf('day'), 'day'));          } else if (mode === 'days-gap') {            const diff = now.diff(target, 'day', true);            value = diff >= 0            ? String(Math.floor(diff) + (includeStart ? 1 : 0))            : String(Math.ceil(-diff));          } else if (mode === 'age') {            value = String(now.diff(target, 'year'));          }        }        if (value !== '') {          el.textContent = value;        }      } catch (error) {        console.error('[LiveDate] Update error:', error);        el.textContent = '日期格式错误';      }    }    function initElements() {      const elements = document.querySelectorAll('.live-date[data-mode]:not([data-initialized])');      elements.forEach(el => {        el.dataset.initialized = 'true';        updateElement(el);        // 每分钟更新一次        setInterval(() => updateElement(el), 60000);      });    }    function startInit() {      ensureDayjs()      .then(() => {        initElements();      })      .catch(error => {        console.error('[LiveDate]', error);        document.querySelectorAll('.live-date[data-mode]:not([data-initialized])').forEach(el => {          el.textContent = '加载失败';          el.dataset.initialized = 'true';        });      });    }    if (document.readyState === 'loading') {      document.addEventListener('DOMContentLoaded', startInit);    } else {      startInit();    }    // 暴露全局重置函数    if (!window.__resetLiveDate) {      window.__resetLiveDate = function() {        document.querySelectorAll('.live-date[data-initialized]').forEach(el => {          delete el.dataset.initialized;        });        if (typeof dayjs !== 'undefined') {          initElements();        }      };    }  })();  </script>    天</p>]]></content>
      
      
      <categories>
          
          <category> 博客相关 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 博客 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>将 UWP 应用部署为 Windows 系统预装应用</title>
      <link href="/20250322/uwp-preinstall-windows/"/>
      <url>/20250322/uwp-preinstall-windows/</url>
      
        <content type="html"><![CDATA[<ol><li><p><strong>准备应用包</strong></p><ul><li>获取**.appx<strong>或</strong>.appxbundle**文件及其依赖项（如VCLibs、.NET框架等）。</li><li>确保应用已使用<strong>受信任的证书签名</strong>（系统级部署需有效签名，测试可用自签名证书）。</li></ul></li><li><p><strong>以管理员身份部署</strong></p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Add-AppxProvisionedPackage命令</span></span><br><span class="line"><span class="built_in">Add-AppxProvisionedPackage</span> <span class="literal">-Online</span> <span class="literal">-PackagePath</span> <span class="string">&quot;C:\Path\To\YourApp.appx&quot;</span> <span class="literal">-DependencyPackagePath</span> <span class="string">&quot;C:\Path\To\Dependencies\*.appx&quot;</span> <span class="literal">-SkipLicense</span></span><br><span class="line"><span class="comment"># DISM命令</span></span><br><span class="line">Dism /Online /<span class="built_in">Add-ProvisionedAppxPackage</span> /PackagePath:<span class="string">&quot;path_to_appx&quot;</span> /DependencyPackagePath:<span class="string">&quot;path_to_dependencies&quot;</span> /SkipLicense</span><br></pre></td></tr></table></figure><ul><li><strong>Online</strong>: 直接部署到当前运行的系统中。</li><li><strong>SkipLicense</strong>: 跳过许可证验证（仅限测试环境）。</li></ul></li><li><p><strong>脱机部署（系统映像集成）</strong></p><p> 若需预装到系统镜像（如OOBE阶段）：</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Add-AppxProvisionedPackage命令</span></span><br><span class="line"><span class="built_in">Add-AppxProvisionedPackage</span> <span class="literal">-SkipLicense</span> <span class="literal">-Path</span> <span class="string">&quot;C:\Mount&quot;</span> <span class="literal">-PackagePath</span> <span class="string">&quot;C:\Path\To\YourApp.appx&quot;</span> <span class="literal">-DependencyPackagePath</span> <span class="string">&quot;C:\Path\To\Dependencies\*.appx&quot;</span></span><br><span class="line"><span class="comment"># DISM命令</span></span><br><span class="line">Dism /<span class="built_in">Add-ProvisionedAppxPackage</span> /ImageFile:<span class="string">&quot;mount_folder&quot;</span> /PackagePath:<span class="string">&quot;path_to_appx&quot;</span> /DependencyPackagePath:<span class="string">&quot;path_to_dependencies&quot;</span> /SkipLicense</span><br></pre></td></tr></table></figure></li></ol><p>这里有一个小脚本来实现一键部署指定文件夹里的所有 .appx 或 .appxbundle 文件部署到系统映像。</p><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 定义镜像路径和包含 Appx 包的文件夹路径</span></span><br><span class="line"><span class="variable">$imagePath</span> = <span class="string">&quot;mount_folder&quot;</span> <span class="comment">#将mount_folder替换为映像位置，如 C:\Mount</span></span><br><span class="line"><span class="variable">$appxFolder</span> = <span class="string">&quot;Appx_folder&quot;</span> <span class="comment">#将Appx_folder替换为APPX位置，如 C:\Test\Apps\MyPackedApp</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取文件夹中所有的 .appx 和 .appxbundle 文件</span></span><br><span class="line"><span class="variable">$appxPackages</span> = <span class="built_in">Get-ChildItem</span> <span class="literal">-Path</span> <span class="variable">$appxFolder</span> <span class="literal">-Filter</span> *.appx <span class="literal">-Recurse</span> <span class="operator">-File</span> |</span><br><span class="line">                <span class="built_in">Select-Object</span> <span class="literal">-ExpandProperty</span> FullName</span><br><span class="line"></span><br><span class="line"><span class="variable">$appxBundles</span> = <span class="built_in">Get-ChildItem</span> <span class="literal">-Path</span> <span class="variable">$appxFolder</span> <span class="literal">-Filter</span> *.appxbundle <span class="literal">-Recurse</span> <span class="operator">-File</span> |</span><br><span class="line">                <span class="built_in">Select-Object</span> <span class="literal">-ExpandProperty</span> FullName</span><br><span class="line"></span><br><span class="line"><span class="comment"># 合并两个列表</span></span><br><span class="line"><span class="variable">$allPackages</span> = <span class="variable">$appxPackages</span> + <span class="variable">$appxBundles</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 遍历每个包并使用 DISM 命令进行安装</span></span><br><span class="line"><span class="keyword">foreach</span> (<span class="variable">$package</span> <span class="keyword">in</span> <span class="variable">$allPackages</span>) &#123;</span><br><span class="line">    <span class="built_in">Write-Host</span> <span class="string">&quot;正在安装包: <span class="variable">$package</span>&quot;</span></span><br><span class="line">    dism.exe /Image:<span class="variable">$imagePath</span> /<span class="built_in">Add-ProvisionedAppxPackage</span> /PackagePath:<span class="string">&quot;<span class="variable">$package</span>&quot;</span> /SkipLicense</span><br><span class="line">    <span class="keyword">if</span> (<span class="variable">$LASTEXITCODE</span> <span class="operator">-ne</span> <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">Write-Error</span> <span class="string">&quot;安装失败: <span class="variable">$package</span>&quot;</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">Write-Host</span> <span class="string">&quot;成功安装: <span class="variable">$package</span>&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>将上述脚本保存为 .ps1 文件并以管理员权限运行它。确保在运行脚本之前，你已正确配置了 $imagePath 和 $appxFolder 变量以匹配你的环境。这样就可以自动化安装文件夹中的所有 appx 文件了。</p><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a><strong>注意事项</strong></h3><ul><li><strong>多用户支持</strong>：系统预装应用将自动为所有用户安装，但首次登录时可能需要初始化。</li><li><strong>更新策略</strong>：系统应用需通过Windows Update或手动更新（需相同证书重新签名后部署）。</li></ul><h3 id="验证部署"><a href="#验证部署" class="headerlink" title="验证部署"></a><strong>验证部署</strong></h3><figure class="highlight powershell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 检查已预装包列表</span></span><br><span class="line"><span class="built_in">Get-AppxProvisionedPackage</span> <span class="literal">-Online</span> | <span class="built_in">Where-Object</span> DisplayName <span class="operator">-eq</span> <span class="string">&quot;YourAppName&quot;</span></span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 系统教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> UWP </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>谷歌小恐龙</title>
      <link href="/20250320/chrome-dino-game/"/>
      <url>/20250320/chrome-dino-game/</url>
      
        <content type="html"><![CDATA[<p>体验：</p><p>(按空格键开始)</p><iframe id="ifrm"  width="80%" height="600px" frameborder="0" src="https://zxclf.us.ci/Chrome_Dino" marginwidth="0" marginheight="0" vspace="0"  hspace="0" allowtransparency="true" scrolling="no" allowfullscreen="true"></iframe>]]></content>
      
      
      <categories>
          
          <category> 游戏娱乐 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Chrome </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Windows 8.1 x64 集成补丁纯净镜像（2025 年最终版）</title>
      <link href="/20250128/windows-8-1-clean-iso/"/>
      <url>/20250128/windows-8-1-clean-iso/</url>
      
        <content type="html"><![CDATA[<h4 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h4><ol><li>本镜像基于MSDN原版 Win8.1 update3 x64专业版，使用Dism++离线制作，<strong>未集成任何第三方软件、无广告、无锁定、无OEM信息。</strong></li><li>本镜像集成Win8.1自发布以来（截止至2025年）的所有关键补丁，并且排除了遥测和Win10升级等无用补丁。</li></ol><h4 id="下载"><a href="#下载" class="headerlink" title="下载"></a>下载</h4><p>123盘：<a href="https://www.123684.com/s/FGWpTd-AZ9Ph">主链接</a> <a href="https://www.123865.com/s/FGWpTd-AZ9Ph">备用链接</a> （无提取码）</p><p>校验信息：</p><p>文件名称: Win8.1.iso<br>文件大小: 3.25 GB (3,501,053,952 字节)<br>MD5: 85222ABA41E54D487B8F571F2E82D192<br>SHA1: 2DB36A34F22F14D35539266906ED8A76426C0BC4<br>SHA256: DA6041B26E41C391C1B34D723B81D36186C6703387B3A84FF0BE258F94E78F98<br>CRC32: DE103C2B</p>]]></content>
      
      
      <categories>
          
          <category> 系统教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Windows 8.1 </tag>
            
            <tag> 系统镜像 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>【最新】Windows 8.1 安装第三方 Metro 应用教程</title>
      <link href="/20250125/windows-8-1-metro-app/"/>
      <url>/20250125/windows-8-1-metro-app/</url>
      
        <content type="html"><![CDATA[<p><strong>注意:</strong></p><p><strong>1.本教程适用于Windows8.1 (build 9600)</strong></p><p><strong>2.折腾有风险，使用本方法可能会产生未知问题，本人概不负责。</strong></p><p>工具下载：<br>123网盘：<a href="https://www.123684.com/s/FGWpTd-Yl9Ph">主链接</a> <a href="https://www.123865.com/s/FGWpTd-Yl9Ph">备用链接</a> (无提取码)</p><p>使用方法:</p><p>下载并解压工具，然后在确保计算机有互联网连接的情况下运行install.bat(弹出的提示可以点击确定或直接关闭)，待脚本运行完成后即可安装APPX。</p>]]></content>
      
      
      <categories>
          
          <category> 系统教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> UWP </tag>
            
            <tag> Windows 8.1 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>使用 WindowsAppBoss 在 Windows 8.x 中安装第三方 Metro 应用</title>
      <link href="/20250120/windowsappboss-appx-guide/"/>
      <url>/20250120/windowsappboss-appx-guide/</url>
      
        <content type="html"><![CDATA[<p>工具下载：<br>123网盘：<a href="https://www.123684.com/s/FGWpTd-309Ph">主链接</a> <a href="https://www.123684.com/s/FGWpTd-309Ph">备用链接</a> (无提取码)</p><p><strong>注意:使用本方法安装第三方Metro应用需要LOB侧载为开启状态</strong></p><h3 id="1-导入APPX的证书"><a href="#1-导入APPX的证书" class="headerlink" title="1.导入APPX的证书"></a>1.导入APPX的证书</h3><p>使用WindowsAppBoss安装第三方Metro应用（文件扩展名为APPX）之前，首先需要导入APPX的证书</p><p>打开APPX的属性，然后点击数字签名选项卡</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_11.png"></p><p>点击详细信息</p><p>在详细信息窗口中点击查看证书</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_12.png"></p><p>点击安装证书</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_13.png"></p><p>储存位置选择本地计算机</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_14.png"></p><p>点击下一步</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_15.png"></p><p>在证书储存中选择将所有证书都放入下列储存</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_16.png"></p><p>点击浏览</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_17.png"></p><p>在弹出的窗口中选择受信任的根证书颁发机构</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_18.png"></p><p>点击确定</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_19.png"></p><p>点击下一步</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_20.png"></p><p>点击完成</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_ZW.png"></p><p>若弹出导入成功的提示说明证书导入成功</p><h3 id="2-使用WindowsAppBoss安装APPX"><a href="#2-使用WindowsAppBoss安装APPX" class="headerlink" title="2.使用WindowsAppBoss安装APPX"></a>2.使用WindowsAppBoss安装APPX</h3><p>以管理员身份运行WindowsAppBoss，点击Install<br><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_22.png"><br>在弹出的窗口中点击Enable side loading<br><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_23.png"></p><p>点击Browse</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_24.png"></p><p>在弹出的窗口中选择APPX的位置</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_25.png"></p><p>选择完成后点击Add Package</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_26.png"></p><p>若窗口中出现Task succeeded则代表安装成功</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_27.png"></p><p>如果安装成功就可以在开始菜单中找到刚刚安装的APPX</p><p><img src="https://raw.githubusercontent.com/ZXCLF/blog_pic/main/1_28.png"></p>]]></content>
      
      
      <categories>
          
          <category> 系统教程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> UWP </tag>
            
            <tag> Windows 8.1 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
